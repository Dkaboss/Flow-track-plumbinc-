<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="theme-color" content="#161b22">
<title>PLUMBinc — FlowTrack Pro</title>
<link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@400;600;700;800&family=Lato:ital,wght@0,300;0,400;0,700;1,300&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
:root {
  --bg: #0d1117;
  --surface: #161b22;
  --surface2: #1c2330;
  --surface3: #222d3a;
  --border: #2a3a4a;
  --border2: #1e2d3d;
  --text: #e6edf3;
  --text2: #8b949e;
  --text3: #4a5568;
  --copper: #e8821a;
  --copper2: #f59840;
  --copper-dim: rgba(232,130,26,0.12);
  --copper-glow: rgba(232,130,26,0.25);
  --green: #3fb950;
  --green-dim: rgba(63,185,80,0.12);
  --red: #f85149;
  --red-dim: rgba(248,81,73,0.12);
  --amber: #d29922;
  --amber-dim: rgba(210,153,34,0.12);
  --blue: #58a6ff;
  --blue-dim: rgba(88,166,255,0.12);
  --radius: 8px;
  --radius-sm: 5px;
  --shadow: 0 4px 24px rgba(0,0,0,0.4);
  --shadow-sm: 0 2px 8px rgba(0,0,0,0.3);
}
*{box-sizing:border-box;margin:0;padding:0;-webkit-tap-highlight-color:transparent}
html{height:100%;height:-webkit-fill-available}
body{font-family:'Lato',sans-serif;background:var(--bg);color:var(--text);font-size:14px;line-height:1.5;height:100%;min-height:100%;min-height:-webkit-fill-available;overflow:hidden;-webkit-text-size-adjust:100%;}

/* ── LAYOUT ── */
.app{display:flex;height:100vh;overflow:hidden}
.sidebar{width:230px;background:var(--surface);border-right:1px solid var(--border2);display:flex;flex-direction:column;flex-shrink:0;overflow-y:auto;-webkit-overflow-scrolling:touch}
.main-wrap{flex:1;display:flex;flex-direction:column;overflow:hidden}
.topbar{background:var(--surface);border-bottom:1px solid var(--border2);padding:0 24px;height:58px;display:flex;align-items:center;gap:14px;flex-shrink:0}
.content{flex:1;overflow-y:auto;padding:24px;-webkit-overflow-scrolling:touch}

/* ── SIDEBAR ── */
.brand{padding:14px 12px 10px;border-bottom:1px solid var(--border2);background:var(--surface2)}
.brand-name{font-family:'Barlow Condensed',sans-serif;font-size:22px;font-weight:800;color:var(--text);letter-spacing:1px;line-height:1}
.brand-name span{color:var(--copper)}
.brand-sub{font-size:10px;letter-spacing:2.5px;text-transform:uppercase;color:var(--text3);margin-top:4px;font-family:'JetBrains Mono',monospace}

.nav-section{font-size:10px;font-weight:700;letter-spacing:2px;text-transform:uppercase;color:var(--text3);padding:14px 18px 5px;font-family:'JetBrains Mono',monospace}
.nav-item{display:flex;align-items:center;gap:10px;padding:10px 18px;cursor:pointer;color:var(--text2);font-size:13.5px;font-weight:400;transition:all .15s;border-left:3px solid transparent;user-select:none;touch-action:manipulation;min-height:44px}
.nav-item:hover{color:var(--text);background:rgba(255,255,255,0.03)}
.nav-item.active{color:var(--copper2);background:var(--copper-dim);border-left-color:var(--copper);font-weight:700}
.nav-icon{font-size:15px;width:18px;text-align:center;flex-shrink:0}
.nav-badge{margin-left:auto;background:var(--copper);color:white;font-size:10px;font-weight:700;padding:1px 7px;border-radius:10px;font-family:'JetBrains Mono',monospace}

.sidebar-footer{margin-top:auto;padding:14px 18px;border-top:1px solid var(--border2)}
.co-chip{display:flex;align-items:center;gap:10px}
.co-avatar{width:32px;height:32px;background:var(--copper);border-radius:6px;display:flex;align-items:center;justify-content:center;font-family:'Barlow Condensed',sans-serif;font-size:17px;font-weight:800;color:white;flex-shrink:0}
.co-name{font-size:12px;font-weight:700;color:var(--text)}
.co-role{font-size:11px;color:var(--text3)}

/* ── TOPBAR ── */
.page-title{font-family:'Barlow Condensed',sans-serif;font-size:24px;font-weight:700;letter-spacing:.5px;color:var(--text);flex:1}
.search-wrap{position:relative;width:280px}
.search-wrap input{width:100%;background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);padding:7px 12px 7px 36px;font-family:'Lato',sans-serif;font-size:13px;color:var(--text);outline:none;transition:border-color .15s}
.search-wrap input:focus{border-color:var(--copper)}
.search-wrap input::placeholder{color:var(--text3)}
.search-icon{position:absolute;left:11px;top:50%;transform:translateY(-50%);font-size:14px;color:var(--text3);pointer-events:none}
.search-results-dropdown{position:absolute;top:calc(100% + 4px);left:0;right:0;background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius);box-shadow:var(--shadow);z-index:999;max-height:280px;overflow-y:auto;display:none}
.search-results-dropdown.open{display:block}
.search-result-item{padding:10px 14px;cursor:pointer;border-bottom:1px solid var(--border2);transition:background .1s}
.search-result-item:last-child{border-bottom:none}
.search-result-item:hover{background:var(--surface3)}
.sr-name{font-weight:700;font-size:13px;color:var(--text)}
.sr-addr{font-size:12px;color:var(--text2)}

/* ── BUTTONS ── */
.btn{display:inline-flex;align-items:center;gap:7px;padding:8px 16px;border-radius:var(--radius-sm);font-family:'Lato',sans-serif;font-size:13px;font-weight:700;cursor:pointer;border:none;transition:all .15s;white-space:nowrap;letter-spacing:.2px;touch-action:manipulation;-webkit-touch-callout:none;user-select:none;min-height:36px}
.btn-primary{background:var(--copper);color:white}
.btn-primary:hover{background:var(--copper2)}
.btn-secondary{background:var(--surface2);color:var(--text2);border:1px solid var(--border)}
.btn-secondary:hover{background:var(--surface3);color:var(--text)}
.btn-ghost{background:transparent;color:var(--text2)}
.btn-ghost:hover{background:var(--surface2);color:var(--text)}
.btn-green{background:var(--green);color:white}
.btn-green:hover{filter:brightness(1.1)}
.btn-danger{background:var(--red-dim);color:var(--red);border:1px solid rgba(248,81,73,.3)}
.btn-danger:hover{background:var(--red);color:white}
.btn-sm{padding:5px 11px;font-size:12px}
.btn-xs{padding:3px 9px;font-size:11px}

/* ── PAGES ── */
.page{display:none}
.page.active{display:block}

/* ── STAT CARDS ── */
.stats-row{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin-bottom:24px}
.stat-card{background:var(--surface);border:1px solid var(--border2);border-radius:var(--radius);padding:18px 20px}
.stat-label{font-size:10px;font-weight:700;letter-spacing:2px;text-transform:uppercase;color:var(--text3);margin-bottom:8px;font-family:'JetBrains Mono',monospace}
.stat-value{font-family:'Barlow Condensed',sans-serif;font-size:42px;font-weight:700;line-height:1;letter-spacing:.5px}
.stat-value.copper{color:var(--copper)}
.stat-value.green{color:var(--green)}
.stat-value.amber{color:var(--amber)}
.stat-value.blue{color:var(--blue)}
.stat-sub{font-size:12px;color:var(--text3);margin-top:4px}

/* ── SECTION HEADER ── */
.sec-header{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px}
.sec-title{font-family:'Barlow Condensed',sans-serif;font-size:20px;font-weight:700;letter-spacing:.5px;color:var(--text)}

/* ── TABLE CARD ── */
.card{background:var(--surface);border:1px solid var(--border2);border-radius:var(--radius);overflow:hidden}
.table-scroll{overflow-x:auto}
table{width:100%;border-collapse:collapse}
thead th{background:var(--surface2);padding:10px 14px;text-align:left;font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:var(--text3);border-bottom:1px solid var(--border2);white-space:nowrap;font-family:'JetBrains Mono',monospace}
tbody td{padding:11px 14px;border-bottom:1px solid var(--border2);font-size:13.5px;vertical-align:middle}
tbody tr:last-child td{border-bottom:none}
tbody tr{cursor:pointer;transition:background .1s}
tbody tr:hover{background:var(--surface2)}
.cl-name{font-weight:700;color:var(--text)}
.cl-sub{font-size:11px;color:var(--text3);margin-top:2px}

/* ── BADGES ── */
.badge{display:inline-flex;align-items:center;gap:5px;padding:3px 9px;border-radius:20px;font-size:11px;font-weight:700;letter-spacing:.2px}
.b-green{background:var(--green-dim);color:var(--green)}
.b-amber{background:var(--amber-dim);color:var(--amber)}
.b-red{background:var(--red-dim);color:var(--red)}
.b-copper{background:var(--copper-dim);color:var(--copper2)}
.b-blue{background:var(--blue-dim);color:var(--blue)}
.b-grey{background:rgba(139,148,158,0.12);color:var(--text2)}
.dot{width:6px;height:6px;border-radius:50%;background:currentColor;display:inline-block}

/* ── MODAL ── */
.overlay{position:fixed;inset:0;background:rgba(0,0,0,.72);z-index:500;display:none;align-items:flex-start;justify-content:center;padding:24px 12px 40px;overflow-y:auto;-webkit-overflow-scrolling:touch;-webkit-backdrop-filter:blur(4px);backdrop-filter:blur(4px)}
.overlay.open{display:flex}
.modal{background:var(--surface);border:1px solid var(--border);border-radius:12px;width:100%;max-width:640px;box-shadow:var(--shadow);animation:mIn .2s ease;margin:auto}
.modal-lg{max-width:820px}
.modal-xl{max-width:900px}
@keyframes mIn{from{opacity:0;transform:translateY(-10px) scale(.98)}to{opacity:1;transform:none}}
.modal-head{display:flex;align-items:center;justify-content:space-between;padding:20px 24px 16px;border-bottom:1px solid var(--border2)}
.modal-title{font-family:'Barlow Condensed',sans-serif;font-size:22px;font-weight:700;letter-spacing:.5px}
.modal-close{width:44px;height:44px;min-width:44px;min-height:44px;display:flex;align-items:center;justify-content:center;background:var(--red-dim);border:1.5px solid rgba(248,81,73,.35);border-radius:8px;cursor:pointer;font-size:18px;color:var(--red);transition:background .15s;touch-action:manipulation;-webkit-touch-callout:none;font-weight:700}
.modal-close:hover{background:var(--red);color:white}
.modal-close:active{background:var(--red);color:white;transform:scale(.95)}
.modal-body{padding:22px 24px}
.modal-body.scrollable{max-height:72vh;overflow-y:auto;-webkit-overflow-scrolling:touch}
.modal-foot{padding:14px 24px;border-top:1px solid var(--border2);display:flex;justify-content:flex-end;gap:10px}
.modal-foot.space{justify-content:space-between}

/* ── TABS ── */
.tabs{display:flex;border-bottom:1px solid var(--border2);padding:0 24px}
.tab{padding:11px 16px;font-size:13px;font-weight:700;color:var(--text3);cursor:pointer;border-bottom:2px solid transparent;margin-bottom:-1px;transition:all .15s;white-space:nowrap}
.tab:hover{color:var(--text2)}
.tab.active{color:var(--copper2);border-bottom-color:var(--copper)}
.pane{display:none;padding:22px 24px}
.pane.active{display:block}

/* ── FORMS ── */
.fg{display:flex;flex-direction:column;gap:6px}
.fg label{font-size:11px;font-weight:700;letter-spacing:.5px;text-transform:uppercase;color:var(--text3);font-family:'JetBrains Mono',monospace}
.fg input,.fg textarea,.fg select{font-family:'Lato',sans-serif;font-size:13.5px;color:var(--text);background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);padding:8px 12px;outline:none;transition:border-color .15s;width:100%}
.fg input:focus,.fg textarea:focus,.fg select:focus{border-color:var(--copper);background:var(--surface3);box-shadow:0 0 0 3px var(--copper-glow)}
.fg textarea{resize:vertical;min-height:72px}
.fg select option{background:var(--surface2)}
.grid2{display:grid;grid-template-columns:1fr 1fr;gap:14px}
.grid3{display:grid;grid-template-columns:1fr 1fr 1fr;gap:14px}
.full{grid-column:1/-1}
.fsec{grid-column:1/-1;font-size:10px;font-weight:700;letter-spacing:2px;text-transform:uppercase;color:var(--text3);padding-bottom:8px;border-bottom:1px solid var(--border2);margin-top:6px;font-family:'JetBrains Mono',monospace}

/* ── INFO GRID ── */
.info-grid{display:grid;grid-template-columns:1fr 1fr 1fr;gap:14px;margin-bottom:20px}
.ii-label{font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:var(--text3);margin-bottom:3px;font-family:'JetBrains Mono',monospace}
.ii-val{font-size:14px;color:var(--text);font-weight:400}

/* ── JOB CARDS ── */
.job-list{display:flex;flex-direction:column;gap:10px}
.job-card{background:var(--surface2);border:1px solid var(--border2);border-radius:var(--radius);padding:14px 16px;cursor:pointer;transition:border-color .15s}
.job-card:hover{border-color:var(--copper)}
.jc-header{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:6px}
.jc-type{font-weight:700;font-size:14px;color:var(--text)}
.jc-date{font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--text3)}
.jc-desc{font-size:13px;color:var(--text2);line-height:1.5}

/* ── PHOTO FOLDER SYSTEM ── */
.photo-folder{background:var(--surface2);border:1px solid var(--border2);border-radius:var(--radius);overflow:hidden;margin-top:12px}
.photo-folder-header{display:flex;align-items:center;gap:10px;padding:10px 14px;border-bottom:1px solid var(--border2);background:var(--surface3)}
.pf-job-id{font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--copper);background:var(--copper-dim);padding:2px 8px;border-radius:4px}
.pf-job-desc{font-size:12px;color:var(--text2);flex:1}
.photo-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(90px,1fr));gap:8px;padding:12px}
.photo-slot{border-radius:6px;overflow:hidden;aspect-ratio:1;position:relative;background:var(--surface3);border:1px solid var(--border2);cursor:pointer}
.photo-slot img{width:100%;height:100%;object-fit:cover;display:block}
.photo-slot .slot-label{position:absolute;bottom:0;left:0;right:0;background:linear-gradient(transparent,rgba(0,0,0,.7));padding:4px 5px 3px;font-size:9px;font-weight:700;letter-spacing:1px;text-transform:uppercase;color:white}
.photo-slot.add-slot{display:flex;flex-direction:column;align-items:center;justify-content:center;border:2px dashed var(--border);cursor:pointer;transition:all .15s;gap:4px}
.photo-slot.add-slot:hover{border-color:var(--copper);background:var(--copper-dim)}
.photo-slot.add-slot span{font-size:20px}
.photo-slot.add-slot small{font-size:9px;color:var(--text3);letter-spacing:.5px;text-align:center}
.photo-section-label{padding:6px 12px;font-size:10px;font-weight:700;letter-spacing:2px;text-transform:uppercase;color:var(--text3);font-family:'JetBrains Mono',monospace;border-bottom:1px solid var(--border2)}

/* ── UPLOAD ZONES ── */
.upload-zone{border:2px dashed var(--border);border-radius:var(--radius-sm);padding:16px;text-align:center;cursor:pointer;transition:all .15s;background:var(--surface2)}
.upload-zone:hover{border-color:var(--copper);background:var(--copper-dim)}
.uz-icon{font-size:24px;margin-bottom:4px}
.uz-text{font-size:12px;color:var(--text3)}
.previews{display:flex;gap:8px;flex-wrap:wrap;margin-top:10px}
.prev-chip{width:64px;height:64px;border-radius:6px;overflow:hidden;border:1px solid var(--border);position:relative;background:var(--surface3)}
.prev-chip img{width:100%;height:100%;object-fit:cover}
.prev-remove{position:absolute;top:2px;right:2px;background:rgba(0,0,0,.7);border:none;border-radius:3px;color:white;font-size:10px;cursor:pointer;padding:1px 4px;line-height:1.2}

/* ── PM CHECKLIST ── */
.pm-section-wrap{margin-bottom:16px}
.pm-section-title{font-family:'Barlow Condensed',sans-serif;font-size:16px;font-weight:700;letter-spacing:.5px;color:var(--copper2);border-bottom:1px solid var(--border2);padding-bottom:6px;margin-bottom:10px;display:flex;align-items:center;gap:8px}
.pm-item{display:flex;align-items:flex-start;gap:10px;padding:7px 10px;background:var(--surface2);border:1px solid var(--border2);border-radius:var(--radius-sm);margin-bottom:6px;cursor:pointer;transition:background .1s}
.pm-item:hover{background:var(--surface3)}
.pm-item.checked{border-color:rgba(63,185,80,.3);background:var(--green-dim)}
.pm-item.na{opacity:.5}
.pm-check{width:18px;height:18px;border-radius:4px;border:2px solid var(--border);flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:11px;color:transparent;transition:all .15s;margin-top:1px}
.pm-item.checked .pm-check{background:var(--green);border-color:var(--green);color:white}
.pm-item-body{flex:1}
.pm-item-label{font-size:13px;font-weight:700;color:var(--text)}
.pm-item.checked .pm-item-label{text-decoration:line-through;color:var(--text3)}
.pm-item-sub{font-size:11px;color:var(--text3);margin-top:2px}
.pm-rating{display:flex;align-items:center;gap:6px;margin-top:5px}
.pm-rating label{font-size:11px;color:var(--text3);white-space:nowrap}
.pm-rating select{font-size:11px;padding:3px 6px;background:var(--surface3);border:1px solid var(--border);color:var(--text);border-radius:4px;outline:none;font-family:'Lato',sans-serif}
.pm-rating select:focus{border-color:var(--copper)}
.pm-note-input{font-size:12px;padding:4px 8px;background:var(--surface3);border:1px solid var(--border);color:var(--text2);border-radius:4px;width:100%;margin-top:5px;font-family:'Lato',sans-serif;outline:none}
.pm-note-input:focus{border-color:var(--copper)}
.pm-progress{height:4px;background:var(--border2);border-radius:2px;margin-bottom:16px;overflow:hidden}
.pm-progress-bar{height:100%;background:var(--green);border-radius:2px;transition:width .3s}

/* ── INSPECTION REPORT ── */
.report-page{background:white;color:#0d1117;font-family:'Lato',sans-serif;padding:36px 40px;max-width:740px;margin:0 auto;line-height:1.6}
.rp-header{display:flex;justify-content:space-between;align-items:flex-start;padding-bottom:18px;border-bottom:3px solid #0d1117;margin-bottom:22px}
.rp-brand{font-family:'Barlow Condensed',sans-serif;font-size:30px;font-weight:800;letter-spacing:1px}
.rp-brand span{color:#e8821a}
.rp-tag{font-size:11px;color:#8b949e;letter-spacing:2px;margin-top:2px}
.rp-id-box{background:#0d1117;color:white;padding:12px 18px;border-radius:8px;text-align:right}
.rp-id-label{font-size:9px;letter-spacing:2px;text-transform:uppercase;color:#4a5568;font-family:'JetBrains Mono',monospace}
.rp-id-value{font-family:'JetBrains Mono',monospace;font-size:13px;margin-top:2px}
.rp-prop{background:#f4f7f9;border:1px solid #c8d8e4;border-radius:8px;padding:14px 18px;margin-bottom:18px;display:grid;grid-template-columns:1fr 1fr;gap:12px}
.rp-il{font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:#8b949e;margin-bottom:2px;font-family:'JetBrains Mono',monospace}
.rp-iv{font-size:13.5px;font-weight:700;color:#0d1117}
.rp-scores{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin:16px 0}
.rp-score-box{text-align:center;padding:14px;border:1px solid #c8d8e4;border-radius:8px}
.rp-score-num{font-family:'Barlow Condensed',sans-serif;font-size:40px;font-weight:700;line-height:1}
.rp-score-label{font-size:11px;color:#8b949e;margin-top:3px;font-weight:700;letter-spacing:.5px}
.green-score{color:#1a7a4a}
.amber-score{color:#c87a10}
.red-score{color:#b83030}
.rp-section-title{font-family:'Barlow Condensed',sans-serif;font-size:15px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:#e8821a;border-bottom:1px solid #e8f0f5;padding-bottom:5px;margin:16px 0 10px}
.rp-finding{display:flex;align-items:flex-start;gap:8px;padding:6px 0;border-bottom:1px solid #f4f7f9;font-size:13px}
.rp-finding:last-child{border-bottom:none}
.rp-fi{flex-shrink:0;font-size:13px;margin-top:1px}
.rp-finding strong{color:#0d1117}
.rp-finding span{color:#4a6075;font-size:12px;margin-left:6px}
.rp-disclaimer{font-size:11px;color:#8b949e;line-height:1.7;background:#f4f7f9;padding:12px 14px;border-radius:6px;margin-top:14px}
.rp-signs{display:grid;grid-template-columns:1fr 1fr;gap:24px;margin-top:24px;padding-top:18px;border-top:1px solid #c8d8e4}
.sign-line{height:38px;border-bottom:1px solid #0d1117;margin-bottom:5px}
.sign-label{font-size:11px;color:#8b949e}
.rp-footer{text-align:center;margin-top:20px;font-size:10px;color:#c8d8e4;font-family:'JetBrains Mono',monospace;letter-spacing:1px;text-transform:uppercase}

/* ── ACTIVITY ── */
.activity-item{display:flex;align-items:center;gap:12px;padding:9px 12px;background:var(--surface2);border:1px solid var(--border2);border-radius:var(--radius-sm);margin-bottom:7px;cursor:pointer;transition:border-color .1s}
.activity-item:hover{border-color:var(--copper)}
.ai-icon{font-size:18px;width:26px;text-align:center;flex-shrink:0}
.ai-body{flex:1}
.ai-title{font-weight:700;font-size:13px;color:var(--text)}
.ai-sub{font-size:12px;color:var(--text3)}
.ai-time{font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--text3)}

/* ── UTILS ── */
.mono{font-family:'JetBrains Mono',monospace}
.muted{color:var(--text3)}
.divider{height:1px;background:var(--border2);margin:16px 0}
.tag{display:inline-block;background:var(--surface3);color:var(--text2);font-size:11px;font-weight:700;padding:3px 8px;border-radius:4px;letter-spacing:.3px}
.alert{display:flex;align-items:flex-start;gap:10px;padding:10px 14px;border-radius:var(--radius-sm);font-size:13px;font-weight:400;margin-bottom:14px}
.alert-green{background:var(--green-dim);color:var(--green);border:1px solid rgba(63,185,80,.25)}
.alert-amber{background:var(--amber-dim);color:var(--amber);border:1px solid rgba(210,153,34,.25)}
.alert-blue{background:var(--blue-dim);color:var(--blue);border:1px solid rgba(88,166,255,.25)}
.two-col{display:grid;grid-template-columns:1fr 1fr;gap:18px}
.three-col{display:grid;grid-template-columns:2fr 1fr;gap:18px}
::-webkit-scrollbar{width:6px;height:6px}
::-webkit-scrollbar-track{background:transparent}
::-webkit-scrollbar-thumb{background:var(--border);border-radius:3px}
.empty-state{text-align:center;padding:36px 20px;color:var(--text3);font-size:13px}
.empty-icon{font-size:32px;margin-bottom:8px}

/* ── LOGIN SCREEN ── */
#login-screen{position:fixed;inset:0;background:var(--bg);z-index:9999;display:flex;flex-direction:column;align-items:center;justify-content:center;padding:24px;animation:fadeIn .3s ease}
@keyframes fadeIn{from{opacity:0}to{opacity:1}}
#login-screen.hidden{display:none}
.login-logo{font-family:'Barlow Condensed',sans-serif;font-size:48px;font-weight:800;letter-spacing:2px;color:var(--text);line-height:1;margin-bottom:6px}
.login-logo span{color:var(--copper)}
.login-sub{font-size:11px;letter-spacing:3px;text-transform:uppercase;color:var(--text3);font-family:'JetBrains Mono',monospace;margin-bottom:28px;text-align:center}
.login-card{background:var(--surface);border:1px solid var(--border2);border-radius:14px;padding:28px 24px;width:100%;max-width:360px;box-shadow:var(--shadow)}
.login-card-title{font-family:'Barlow Condensed',sans-serif;font-size:20px;font-weight:700;letter-spacing:.5px;margin-bottom:4px}
.login-card-sub{font-size:12px;color:var(--text3);margin-bottom:20px}
.tech-select-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:20px}
.tech-btn{background:var(--surface2);border:2px solid var(--border2);border-radius:10px;padding:14px 10px;text-align:center;cursor:pointer;transition:all .15s;touch-action:manipulation}
.tech-btn:hover,.tech-btn:active{border-color:var(--copper);background:var(--copper-dim)}
.tech-btn.selected{border-color:var(--copper);background:var(--copper-dim)}
.tech-btn-avatar{width:40px;height:40px;border-radius:10px;background:var(--surface3);border:1px solid var(--border);display:flex;align-items:center;justify-content:center;font-family:'Barlow Condensed',sans-serif;font-size:20px;font-weight:800;color:var(--copper2);margin:0 auto 8px}
.tech-btn-name{font-size:12px;font-weight:700;color:var(--text)}
.tech-btn-role{font-size:10px;color:var(--text3);margin-top:2px}
.pin-label{font-size:11px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:var(--text3);font-family:'JetBrains Mono',monospace;margin-bottom:8px}
.pin-dots{display:flex;justify-content:center;gap:12px;margin-bottom:20px}
.pin-dot{width:14px;height:14px;border-radius:50%;border:2px solid var(--border);transition:all .2s}
.pin-dot.filled{background:var(--copper);border-color:var(--copper)}
.pin-dot.error{background:var(--red);border-color:var(--red);animation:shake .3s ease}
@keyframes shake{0%,100%{transform:translateX(0)}25%{transform:translateX(-4px)}75%{transform:translateX(4px)}}
.pin-pad{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}
.pin-key{background:var(--surface2);border:1px solid var(--border2);border-radius:10px;height:56px;display:flex;align-items:center;justify-content:center;font-family:'Barlow Condensed',sans-serif;font-size:24px;font-weight:700;color:var(--text);cursor:pointer;transition:all .12s;touch-action:manipulation;user-select:none;-webkit-touch-callout:none}
.pin-key:active{background:var(--copper);border-color:var(--copper);color:white;transform:scale(.95)}
.pin-key.del{font-size:18px;color:var(--text3)}
.pin-key.del:active{background:var(--red-dim);border-color:var(--red);color:var(--red)}
.login-error{font-size:12px;color:var(--red);text-align:center;margin-bottom:10px;min-height:18px}
.login-back-btn{background:none;border:none;color:var(--text3);font-size:13px;font-family:'Lato',sans-serif;cursor:pointer;margin-top:14px;padding:8px;touch-action:manipulation;display:block;width:100%;text-align:center}
.login-back-btn:active{color:var(--text)}
/* manage techs modal bits */
.tech-row{display:flex;align-items:center;gap:12px;padding:10px 0;border-bottom:1px solid var(--border2)}
.tech-row:last-child{border-bottom:none}
.tech-row-avatar{width:34px;height:34px;border-radius:8px;background:var(--surface3);display:flex;align-items:center;justify-content:center;font-family:'Barlow Condensed',sans-serif;font-size:17px;font-weight:800;color:var(--copper2);flex-shrink:0}
.tech-row-info{flex:1}
.tech-row-name{font-weight:700;font-size:13px}
.tech-row-pin{font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--text3)}


/* ── STICKY MODAL CLOSE BAR (mobile) ── */
.modal-sticky-close{display:none}
@media(max-width:768px){
  .modal-sticky-close{display:flex;align-items:center;justify-content:center;padding:10px 16px 16px;border-top:1px solid var(--border2);background:var(--surface);position:sticky;bottom:0;left:0;right:0;z-index:10}
  .modal-sticky-close button{width:100%;height:46px;background:var(--red-dim);border:1.5px solid rgba(248,81,73,.4);border-radius:10px;color:var(--red);font-family:'Lato',sans-serif;font-size:15px;font-weight:700;cursor:pointer;touch-action:manipulation;display:flex;align-items:center;justify-content:center;gap:8px}
  .modal-sticky-close button:active{background:var(--red);color:white}
}

/* ── MODAL DRAG HANDLE ── */
.modal::before{content:'';display:none}
@media(max-width:768px){
  .modal::before{display:block;width:40px;height:4px;background:var(--border);border-radius:2px;margin:10px auto 0;flex-shrink:0}
}
/* ── CALENDAR ── */
.cal-dow-row{display:grid;grid-template-columns:repeat(7,1fr);gap:4px;margin-bottom:4px}
.cal-dow-row div{text-align:center;font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:var(--text3);padding:6px 0;font-family:'JetBrains Mono',monospace}
.cal-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:4px}
.cal-cell{background:var(--surface);border:1px solid var(--border2);border-radius:var(--radius-sm);min-height:110px;padding:6px 7px;display:flex;flex-direction:column;gap:3px;transition:border-color .15s;position:relative}
.cal-cell:hover{border-color:var(--border)}
.cal-cell.today{border-color:var(--copper)!important;background:var(--copper-dim)}
.cal-cell.other-month{opacity:.35}
.cal-day-num{font-family:'JetBrains Mono',monospace;font-size:12px;font-weight:700;color:var(--text2);line-height:1;flex-shrink:0;margin-bottom:2px}
.cal-cell.today .cal-day-num{color:var(--copper2)}
.cal-job-bar{border-radius:4px;padding:3px 7px;font-size:11px;font-weight:700;color:white;cursor:pointer;transition:filter .15s;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;line-height:1.4}
.cal-job-bar:hover{filter:brightness(1.2)}
.cal-job-bar.completed{background:var(--green)}
.cal-job-bar.in-progress{background:var(--amber)}
.cal-job-bar.not-done{background:var(--red)}
.cal-more{font-size:10px;color:var(--text3);font-family:'JetBrains Mono',monospace;padding:2px 4px;cursor:pointer;text-align:center}
.cal-more:hover{color:var(--copper2)}
.cal-day-panel{position:fixed;top:50%;left:50%;transform:translate(-50%,-50%);background:var(--surface);border:1px solid var(--border);border-radius:12px;width:440px;max-width:95vw;box-shadow:var(--shadow);z-index:600;animation:mIn .2s ease}
.cal-day-panel-head{display:flex;align-items:center;justify-content:space-between;padding:16px 20px;border-bottom:1px solid var(--border2)}
.cal-day-panel-body{padding:16px 20px;max-height:60vh;overflow-y:auto;-webkit-overflow-scrolling:touch}

/* ── MOBILE / IOS ── */
@media (max-width: 768px) {
  .app{flex-direction:column}
  .sidebar{width:100%;height:auto;border-right:none;border-bottom:1px solid var(--border2);flex-direction:row;overflow-x:auto;overflow-y:hidden;-webkit-overflow-scrolling:touch}
  .sidebar nav{display:flex;flex-direction:row;padding:0;white-space:nowrap;overflow-x:auto}
  .nav-section{display:none}
  .nav-item{padding:12px 14px;border-left:none;border-bottom:3px solid transparent;min-height:48px;flex-shrink:0}
  .nav-item.active{border-left:none;border-bottom-color:var(--copper)}
  .nav-badge{display:none}
  .brand{padding:12px 16px;white-space:nowrap;flex-shrink:0}
  .brand-name{font-size:18px}
  .brand-sub{display:none}
  .sidebar-footer{display:none}
  .main-wrap{flex:1;min-height:0}
  .topbar{padding:0 14px;height:52px;gap:10px}
  .page-title{font-size:18px}
  .search-wrap{width:160px}
  .content{padding:14px}
  .stats-row{grid-template-columns:1fr 1fr;gap:10px}
  .two-col{grid-template-columns:1fr;gap:14px}
  .info-grid{grid-template-columns:1fr 1fr}
  .grid2{grid-template-columns:1fr}
  .grid3{grid-template-columns:1fr}
  .full{grid-column:1}
  .modal{border-radius:12px 12px 0 0;margin:auto 0 0;max-height:92vh}
  .modal-lg,.modal-xl{max-width:100%}
  .overlay{padding:0;align-items:flex-end}
  .modal-body.scrollable{max-height:60vh}
  .cal-cell{min-height:70px}
  .cal-job-bar{font-size:10px;padding:2px 5px}
  .cal-day-panel{width:95vw;top:auto;bottom:0;left:0;transform:none;border-radius:12px 12px 0 0;position:fixed}
  .tabs{overflow-x:auto;-webkit-overflow-scrolling:touch}
  .tab{white-space:nowrap;font-size:12px;padding:10px 12px}
  table{font-size:12px}
  thead th{font-size:9px;padding:8px 10px}
  tbody td{padding:8px 10px}
}
@media (max-width:480px){
  .stats-row{grid-template-columns:1fr 1fr}
  .stat-value{font-size:32px}
  .search-wrap{width:130px}
  .search-wrap input{font-size:12px}
  .cal-dow-row div{font-size:9px}
  .cal-cell{min-height:56px;padding:4px}
  .cal-day-num{font-size:11px}
}
</style>
</head>
<body>
<div class="app">

<!-- SIDEBAR -->
<aside class="sidebar">
  <div class="brand">
    <svg viewBox="0 0 210 56" xmlns="http://www.w3.org/2000/svg" style="width:190px;display:block;margin:0 auto">
      <defs>
        <linearGradient id="sbFlame" x1="0" y1="0" x2="0" y2="1">
          <stop offset="0%" stop-color="#f59410"/>
          <stop offset="55%" stop-color="#e8520a"/>
          <stop offset="100%" stop-color="#bf2e08"/>
        </linearGradient>
        <linearGradient id="sbInner" x1="0" y1="0" x2="0" y2="1">
          <stop offset="0%" stop-color="#ffd878"/>
          <stop offset="100%" stop-color="#f59410"/>
        </linearGradient>
        <linearGradient id="sbBlue" x1="0" y1="0" x2="0" y2="1">
          <stop offset="0%" stop-color="#2255a0"/>
          <stop offset="100%" stop-color="#0f3575"/>
        </linearGradient>
      </defs>
      <!-- Blue shield/drop background -->
      <ellipse cx="24" cy="30" rx="18" ry="22" fill="url(#sbBlue)" opacity="0.85"/>
      <!-- Flame outer -->
      <path d="M24 5 C24 5 8 20 8 33 C8 42.5 15.2 50 24 50 C32.8 50 40 42.5 40 33 C40 20 24 5 24 5Z" fill="url(#sbFlame)"/>
      <!-- Flame inner highlight -->
      <path d="M24 18 C24 18 15 29 15 35 C15 40 19 44 24 44 C29 44 33 40 33 35 C33 29 24 18 24 18Z" fill="url(#sbInner)"/>
      <!-- White hot core -->
      <ellipse cx="24" cy="36" rx="5" ry="6.5" fill="white" opacity="0.88"/>
      <!-- PLUMB text -->
      <text x="48" y="33" font-family="'Arial Black',Arial" font-weight="900" font-size="23" fill="white" letter-spacing="0.8">PLUMB</text>
      <!-- inc italic orange -->
      <text x="155" y="33" font-family="'Arial Black',Arial" font-weight="900" font-size="23" fill="#f59410" letter-spacing="0.5" font-style="italic">inc</text>
      <!-- tagline -->
      <text x="48" y="48" font-family="Arial,sans-serif" font-size="9" fill="#5a9ad4" letter-spacing="2.2">PLUMBING &amp; GAS</text>
    </svg>
    <div style="text-align:center;font-size:9px;letter-spacing:2px;text-transform:uppercase;color:var(--text3);font-family:'JetBrains Mono',monospace;margin-top:3px">FlowTrack Pro</div>
  </div>
  <nav>
    <div class="nav-section">Main</div>
    <div class="nav-item active" data-page="dashboard" onclick="nav(this)"><span class="nav-icon">⊞</span>Dashboard</div>
    <div class="nav-item" data-page="clients" onclick="nav(this)"><span class="nav-icon">👤</span>Clients <span class="nav-badge" id="nb-clients">0</span></div>
    <div class="nav-item" data-page="jobs" onclick="nav(this)"><span class="nav-icon">🔧</span>Job History</div>
    <div class="nav-item" data-page="photos" onclick="nav(this)"><span class="nav-icon">📁</span>Photo Folders</div>
    <div class="nav-section">Inspections</div>
    <div class="nav-item" data-page="partners" onclick="nav(this)"><span class="nav-icon">🏠</span>RE Partners</div>
    <div class="nav-item" data-page="inspections" onclick="nav(this)"><span class="nav-icon">📋</span>Inspections <span class="nav-badge" id="nb-insp">0</span></div>
    <div class="nav-section">Schedule</div>
    <div class="nav-item" data-page="calendar" onclick="nav(this)"><span class="nav-icon">📅</span>Calendar</div>
    <div class="nav-section">Maintenance</div>
    <div class="nav-item" data-page="pm" onclick="nav(this)"><span class="nav-icon">✅</span>PM Checklists</div>
  </nav>
  <div class="sidebar-footer">
    <div class="co-chip">
      <div class="co-avatar">P</div>
      <div><div class="co-name">Plumb.Inc</div><div class="co-role">Administrator</div></div>
    </div>
  </div>
</aside>

<!-- MAIN -->
<div class="main-wrap">
  <header class="topbar">
    <div class="page-title" id="page-title">Dashboard</div>
    <div class="search-wrap">
      <span class="search-icon">🔍</span>
      <input type="text" id="search-input" placeholder="Search clients by name or address…" oninput="liveSearch(this.value)" autocomplete="off">
      <div class="search-results-dropdown" id="search-dropdown"></div>
    </div>
    <button class="btn btn-primary" id="topbar-btn" onclick="topbarAction()" style="display:none">＋ New</button>
  </header>

  <div class="content">

    <!-- DASHBOARD -->
    <div class="page active" id="page-dashboard">
      <div class="stats-row">
        <div class="stat-card"><div class="stat-label">Clients</div><div class="stat-value copper" id="s-clients">0</div><div class="stat-sub">In database</div></div>
        <div class="stat-card"><div class="stat-label">Jobs Done</div><div class="stat-value green" id="s-jobs">0</div><div class="stat-sub">All time</div></div>
        <div class="stat-card"><div class="stat-label">PM Overdue</div><div class="stat-value amber" id="s-pm">0</div><div class="stat-sub">&gt; 6 months</div></div>
        <div class="stat-card"><div class="stat-label">Inspections</div><div class="stat-value blue" id="s-insp">0</div><div class="stat-sub">Reports issued</div></div>
      </div>
      <div class="two-col">
        <div>
          <div class="sec-header"><div class="sec-title">Recent Jobs</div></div>
          <div id="dash-recent"></div>
        </div>
        <div>
          <div class="sec-header"><div class="sec-title">PM Alerts</div></div>
          <div id="dash-pm-alerts"></div>
        </div>
      </div>
    </div>

    <!-- CLIENTS -->
    <div class="page" id="page-clients">
      <div class="sec-header">
        <div class="sec-title">Client Database</div>
        <div style="display:flex;gap:8px">
          <button class="btn btn-secondary btn-sm" onclick="exportCSV()">↓ CSV</button>
          <button class="btn btn-primary" onclick="openAddClient()">＋ Add Client</button>
        </div>
      </div>
      <div class="card table-scroll">
        <table><thead><tr>
          <th>Client / Property</th><th>Phone</th><th>Type</th><th>Last Visit</th><th>Last Issue</th><th>PM Status</th><th></th>
        </tr></thead>
        <tbody id="clients-tbody"></tbody></table>
      </div>
    </div>

    <!-- JOBS -->
    <div class="page" id="page-jobs">
      <div class="sec-header">
        <div class="sec-title">Job History</div>
        <button class="btn btn-primary" onclick="openAddJob()">＋ Log Job</button>
      </div>
      <div class="card table-scroll">
        <table><thead><tr>
          <th>Job ID</th><th>Date</th><th>Client</th><th>Type</th><th>Description</th><th>Status</th><th>Photos</th><th></th>
        </tr></thead>
        <tbody id="jobs-tbody"></tbody></table>
      </div>
    </div>

    <!-- PHOTO FOLDERS -->
    <div class="page" id="page-photos">
      <div class="sec-header">
        <div class="sec-title">Photo Folders — Linked to Jobs</div>
        <div style="font-size:12px;color:var(--text3)">Every job has its own folder. Photos are tagged Before / After.</div>
      </div>
      <div id="photo-folders-list"></div>
    </div>

    <!-- RE PARTNERS -->
    <div class="page" id="page-partners">
      <div class="sec-header">
        <div class="sec-title">Real Estate Partners</div>
        <button class="btn btn-primary" onclick="openAddPartner()">＋ Add Partner</button>
      </div>
      <div class="alert alert-green">🏠 All properties from RE partners receive a <strong>complimentary plumbing &amp; gas inspection</strong>.</div>
      <div class="card table-scroll">
        <table><thead><tr>
          <th>Agency / Contact</th><th>Phone / Email</th><th>Referred</th><th>Inspections</th><th>Status</th><th></th>
        </tr></thead>
        <tbody id="partners-tbody"></tbody></table>
      </div>
    </div>

    <!-- INSPECTIONS -->
    <div class="page" id="page-inspections">
      <div class="sec-header">
        <div class="sec-title">Inspection Reports</div>
        <button class="btn btn-primary" onclick="openNewInspection()">＋ New Inspection</button>
      </div>
      <div class="card table-scroll">
        <table><thead><tr>
          <th>Report #</th><th>Date</th><th>Address</th><th>Agency</th><th>Plumbing</th><th>Gas</th><th>Overall</th><th></th>
        </tr></thead>
        <tbody id="insp-tbody"></tbody></table>
      </div>
    </div>

    <!-- PM CHECKLISTS -->
    <div class="page" id="page-pm">
      <div class="sec-header">
        <div class="sec-title">PM Checklists</div>
        <button class="btn btn-primary" onclick="openNewPM()">＋ New PM Check</button>
      </div>
      <div id="pm-records-list"></div>
    </div>

    <!-- CALENDAR -->
    <div class="page" id="page-calendar">
      <div class="sec-header">
        <div style="display:flex;align-items:center;gap:14px">
          <button class="btn btn-secondary btn-sm" onclick="calNav(-1)">‹ Prev</button>
          <div class="sec-title" id="cal-month-label" style="min-width:200px;text-align:center"></div>
          <button class="btn btn-secondary btn-sm" onclick="calNav(1)">Next ›</button>
          <button class="btn btn-ghost btn-sm" onclick="calGoToday()">Today</button>
        </div>
        <div style="display:flex;align-items:center;gap:16px;flex-wrap:wrap" id="cal-legend">
          <span style="display:flex;align-items:center;gap:5px;font-size:11px;color:var(--text2)"><span style="font-size:13px">●</span> Completed</span>
          <span style="display:flex;align-items:center;gap:5px;font-size:11px;color:var(--text2)"><span style="font-size:13px">○</span> Outstanding</span>
          <span style="display:flex;align-items:center;gap:5px;font-size:11px;color:var(--text2)"><span style="font-size:13px">◑</span> In Progress</span>
          <span style="color:var(--text3);font-size:11px">· Colour = technician</span>
        </div>
      </div>

      <!-- Day-of-week header -->
      <div class="cal-dow-row">
        <div>Sun</div><div>Mon</div><div>Tue</div><div>Wed</div><div>Thu</div><div>Fri</div><div>Sat</div>
      </div>
      <!-- Calendar grid -->
      <div class="cal-grid" id="cal-grid"></div>
    </div>

  </div>
</div>
</div>

<!-- ═══ MODAL: ADD / EDIT CLIENT ═══ -->
<div class="overlay" id="m-client">
  <div class="modal">
    <div class="modal-head">
      <div class="modal-title" id="m-client-title">New Client</div>
      <button class="modal-close" onclick="close('m-client')">✕</button>
    </div>
    <div class="modal-body scrollable">
      <div class="grid2">
        <div class="fsec">Client Details</div>
        <div class="fg"><label>First Name *</label><input id="cf-fname" placeholder="John"></div>
        <div class="fg"><label>Last Name *</label><input id="cf-lname" placeholder="Smith"></div>
        <div class="fg"><label>Phone *</label><input id="cf-phone" placeholder="04XX XXX XXX"></div>
        <div class="fg"><label>Email</label><input id="cf-email" type="email" placeholder="john@email.com"></div>
        <div class="fg full"><label>Property Address *</label><input id="cf-address" placeholder="12 Maple St, Suburb NSW 2000"></div>
        <div class="fg"><label>Client Type</label>
          <select id="cf-type"><option value="residential">Residential</option><option value="commercial">Commercial</option><option value="real-estate">RE Referral</option></select>
        </div>
        <div class="fg"><label>Referred By</label><input id="cf-ref" placeholder="Agency or person"></div>
        <div class="fsec">Property Info</div>
        <div class="fg"><label>Property Type</label>
          <select id="cf-proptype"><option>House</option><option>Unit / Apartment</option><option>Townhouse</option><option>Commercial</option><option>Other</option></select>
        </div>
        <div class="fg"><label>Year Built (approx)</label><input id="cf-year" placeholder="e.g. 2005"></div>
        <div class="fg full"><label>Notes</label><textarea id="cf-notes" placeholder="Any notes about this client or property…"></textarea></div>
      </div>
    </div>
    <div class="modal-foot">
      <button class="btn btn-secondary" onclick="close('m-client')">Cancel</button>
      <button class="btn btn-primary" onclick="saveClient()">Save Client</button>
    </div>
    <div class="modal-sticky-close"><button onclick="closeModal('m-client')">✕ Close</button></div>
  </div>
</div>

<!-- ═══ MODAL: CLIENT DETAIL ═══ -->
<div class="overlay" id="m-detail">
  <div class="modal modal-xl">
    <div class="modal-head">
      <div>
        <div class="modal-title" id="det-name">—</div>
        <div style="font-size:12px;color:var(--text3)" id="det-addr">—</div>
      </div>
      <div style="display:flex;gap:8px;align-items:center">
        <button class="btn btn-secondary btn-sm" onclick="editCurrentClient()">✏️ Edit</button>
        <button class="btn btn-secondary btn-sm" onclick="openAddJob(currentId)">＋ Job</button>
        <button class="btn btn-primary btn-sm" onclick="openNewInspection(currentId)">＋ Inspection</button>
        <button class="modal-close" onclick="close('m-detail')">✕</button>
      </div>
    </div>
    <div class="tabs">
      <div class="tab active" onclick="tab(this,'tp-overview')">Overview</div>
      <div class="tab" onclick="tab(this,'tp-jobs')">Jobs & Photos</div>
      <div class="tab" onclick="tab(this,'tp-pm')">PM Records</div>
      <div class="tab" onclick="tab(this,'tp-insp')">Inspections</div>
    </div>
    <div class="pane active" id="tp-overview">
      <!-- Next-visit notes banner -->
      <div id="dv-nextnotes-banner"></div>
      <!-- Urgent work banner -->
      <div id="dv-urgent-banner"></div>
      <div class="info-grid">
        <div><div class="ii-label">Phone</div><div class="ii-val" id="dv-phone">—</div></div>
        <div><div class="ii-label">Email</div><div class="ii-val" id="dv-email">—</div></div>
        <div><div class="ii-label">Client Type</div><div class="ii-val" id="dv-type">—</div></div>
        <div><div class="ii-label">Last Visit</div><div class="ii-val mono" id="dv-lv">—</div></div>
        <div><div class="ii-label">Last Issue</div><div class="ii-val" id="dv-li">—</div></div>
        <div><div class="ii-label">Total Jobs</div><div class="ii-val" id="dv-tj">0</div></div>
        <div><div class="ii-label">Property Type</div><div class="ii-val" id="dv-pt">—</div></div>
        <div><div class="ii-label">Year Built</div><div class="ii-val" id="dv-yb">—</div></div>
        <div><div class="ii-label">Referred By</div><div class="ii-val" id="dv-ref">—</div></div>
      </div>
      <div class="ii-label" style="margin-bottom:6px">Notes</div>
      <div id="dv-notes" style="font-size:13.5px;color:var(--text2);background:var(--surface2);padding:12px;border-radius:6px;border:1px solid var(--border2)">—</div>
    </div>
    <div class="pane" id="tp-jobs" style="max-height:65vh;overflow-y:auto;-webkit-overflow-scrolling:touch">
      <div id="det-jobs-list"></div>
    </div>
    <div class="pane" id="tp-pm" style="max-height:65vh;overflow-y:auto;-webkit-overflow-scrolling:touch">
      <div id="det-pm-list"></div>
    </div>
    <div class="pane" id="tp-insp" style="max-height:65vh;overflow-y:auto;-webkit-overflow-scrolling:touch">
      <div id="det-insp-list"></div>
    </div>
    <div class="modal-foot space">
      <button class="btn btn-danger btn-sm" onclick="deleteClient()">🗑 Delete Client</button>
      <button class="btn btn-secondary" onclick="close('m-detail')">Close</button>
    </div>
    <div class="modal-sticky-close"><button onclick="closeModal('m-detail')">✕ Close</button></div>
  </div>
</div>

<!-- ═══ MODAL: LOG JOB ═══ -->
<div class="overlay" id="m-job">
  <div class="modal modal-lg">
    <div class="modal-head">
      <div class="modal-title">Log New Job</div>
      <button class="modal-close" onclick="close('m-job')">✕</button>
    </div>
    <div class="modal-body scrollable">
      <div class="grid2">

        <div class="fsec">Job Details</div>
        <div class="fg full"><label>Client *</label><select id="jf-client"></select></div>
        <div class="fg"><label>Date *</label><input id="jf-date" type="date"></div>
        <div class="fg"><label>Job Type</label>
          <select id="jf-type">
            <option>Service Call</option><option>Repair</option><option>Installation</option>
            <option>Inspection</option><option>PM Check</option><option>Gas Fitting</option>
            <option>Drain Clearing</option><option>Hot Water</option><option>Leak Detection</option><option>Emergency Call</option><option>Other</option>
          </select>
        </div>
        <div class="fg"><label>Arrival Time</label><input id="jf-arrive" type="time" placeholder="08:00"></div>
        <div class="fg"><label>Finish Time</label><input id="jf-finish" type="time" placeholder="10:30"></div>
        <div class="fg"><label>Technician Name *</label><input id="jf-tech" placeholder="Full name"></div>
        <div class="fg"><label>Licence / Cert No.</label><input id="jf-licence" placeholder="e.g. PL12345"></div>
        <div class="fg"><label>Status</label>
          <select id="jf-status"><option value="completed">Completed</option><option value="in-progress">In Progress</option><option value="follow-up">Needs Follow-Up</option></select>
        </div>
        <div class="fg full"><label>Work Performed / Issue Found</label><textarea id="jf-desc" rows="3" placeholder="Describe the job, fault found, work completed, materials used…"></textarea></div>

        <div class="fsec">🚨 Site Findings & Urgent Recommendations</div>
        <div style="grid-column:1/-1">
          <div class="alert alert-amber">Use this section to flag anything found on-site that the client or next technician needs to know about — defects, safety concerns, items outside scope, urgent work recommended.</div>
        </div>
        <div class="fg full"><label>Urgent / Recommended Work</label><textarea id="jf-urgent" rows="3" placeholder="e.g. Hot water system showing signs of corrosion — recommend replacement within 3 months. PRV has not discharged cleanly — schedule inspection…"></textarea></div>
        <div class="fg full"><label>Site Observations / Inspection Findings</label><textarea id="jf-findings" rows="3" placeholder="e.g. Noticed water staining on ceiling near bathroom — possible slow leak above. Roof penetration flashing around soil vent showing cracking…"></textarea></div>

        <div class="fsec">📋 Notes for Next Visit</div>
        <div style="grid-column:1/-1">
          <div class="alert alert-blue">These notes will appear as a visible reminder the next time this client's record is opened or a job is logged.</div>
        </div>
        <div class="fg full"><label>Next Visit Notes / Handover for Tech</label><textarea id="jf-nextnotes" rows="3" placeholder="e.g. Client wants to discuss upgrading tapware next visit. Isolator under kitchen sink is stiff — bring valve key. Dog on premises — side gate entry only…"></textarea></div>
        <div class="fg"><label>Follow-Up Due Date</label><input id="jf-followdate" type="date"></div>
        <div class="fg"><label>Follow-Up Priority</label>
          <select id="jf-followprio">
            <option value="">— None</option>
            <option value="urgent">🔴 Urgent</option>
            <option value="high">🟠 High</option>
            <option value="normal">🟡 Normal</option>
            <option value="low">🟢 Low / Routine</option>
          </select>
        </div>

        <div class="fsec">📁 Job Photos — Auto-Linked to This Job</div>
        <div style="grid-column:1/-1">
          <div class="alert alert-blue">Photos are automatically saved into this job's folder — tagged Before/After and linked to the Job ID forever.</div>
        </div>
        <div class="fg full">
          <label>Before Photos</label>
          <div class="upload-zone" onclick="document.getElementById('jf-before').click()">
            <div class="uz-icon">📸</div><div class="uz-text">Click to upload BEFORE photos</div>
          </div>
          <input type="file" id="jf-before" accept="image/*" multiple style="display:none" onchange="handlePhotos(this,'before')">
          <div class="previews" id="prev-before"></div>
        </div>
        <div class="fg full">
          <label>After Photos</label>
          <div class="upload-zone" onclick="document.getElementById('jf-after').click()">
            <div class="uz-icon">📸</div><div class="uz-text">Click to upload AFTER photos</div>
          </div>
          <input type="file" id="jf-after" accept="image/*" multiple style="display:none" onchange="handlePhotos(this,'after')">
          <div class="previews" id="prev-after"></div>
        </div>

      </div>
    </div>
    <div class="modal-foot">
      <button class="btn btn-secondary" onclick="close('m-job')">Cancel</button>
      <button class="btn btn-primary" onclick="saveJob()">Save Job & Photos</button>
    </div>
    <div class="modal-sticky-close"><button onclick="closeModal('m-job')">✕ Close</button></div>
  </div>
</div>

<!-- ═══ MODAL: ADD PARTNER ═══ -->
<div class="overlay" id="m-partner">
  <div class="modal">
    <div class="modal-head"><div class="modal-title">New RE Partner</div><button class="modal-close" onclick="close('m-partner')">✕</button></div>
    <div class="modal-body">
      <div class="alert alert-green">🏠 All referrals from this partner receive complimentary inspections.</div>
      <div class="grid2">
        <div class="fg"><label>Agency Name *</label><input id="pf-agency" placeholder="Ray White, McGrath…"></div>
        <div class="fg"><label>Contact Person</label><input id="pf-contact" placeholder="Sarah Jones"></div>
        <div class="fg"><label>Phone</label><input id="pf-phone" placeholder="02 XXXX XXXX"></div>
        <div class="fg"><label>Email</label><input id="pf-email" type="email" placeholder="sarah@agency.com.au"></div>
        <div class="fg full"><label>Notes</label><textarea id="pf-notes" placeholder="Area, agreement details…"></textarea></div>
      </div>
    </div>
    <div class="modal-foot">
      <button class="btn btn-secondary" onclick="close('m-partner')">Cancel</button>
      <button class="btn btn-primary" onclick="savePartner()">Save Partner</button>
    </div>
    <div class="modal-sticky-close"><button onclick="closeModal('m-partner')">✕ Close</button></div>
  </div>
</div>

<!-- ═══ MODAL: NEW INSPECTION ═══ -->
<div class="overlay" id="m-insp">
  <div class="modal modal-lg">
    <div class="modal-head"><div class="modal-title">New Inspection Report</div><button class="modal-close" onclick="close('m-insp')">✕</button></div>
    <div class="modal-body scrollable">
      <div class="alert alert-green">🏠 Complimentary inspection — presented to your RE partner on completion.</div>
      <div class="grid2">
        <div class="fsec">Property & Assignment</div>
        <div class="fg full"><label>Property Address *</label><input id="inf-addr" placeholder="12 Maple St, Suburb NSW 2000"></div>
        <div class="fg"><label>Inspection Date *</label><input id="inf-date" type="date"></div>
        <div class="fg"><label>RE Agency</label><select id="inf-agency"><option value="">-- Select --</option></select></div>
        <div class="fg"><label>Agent Name</label><input id="inf-agent" placeholder="Agent full name"></div>
        <div class="fg"><label>Agent Phone / Email</label><input id="inf-agentcontact" placeholder="04XX XXX XXX or email@agency.com"></div>
        <div class="fg"><label>Property Type</label>
          <select id="inf-proptype"><option>House</option><option>Unit / Apartment</option><option>Townhouse</option><option>Commercial</option><option>Other</option></select>
        </div>
        <div class="fg"><label>Year Built (approx)</label><input id="inf-year" placeholder="e.g. 1998"></div>
        <div class="fsec">Inspector & Site Details</div>
        <div class="fg"><label>Inspector Name *</label><input id="inf-insp" placeholder="Full name"></div>
        <div class="fg"><label>Plumbing Licence No.</label><input id="inf-licence" placeholder="e.g. PL12345"></div>
        <div class="fg"><label>Arrival Time</label><input id="inf-arrive" type="time"></div>
        <div class="fg"><label>Finish Time</label><input id="inf-finish" type="time"></div>
        <div class="fg full"><label>Weather / Site Conditions</label><input id="inf-conditions" placeholder="e.g. Dry, sunny. Property vacant. Utilities on."></div>
        <div class="fsec">Plumbing Assessment</div>
        <div class="fg"><label>Hot Water System</label><select id="i-hws"><option value="pass">✅ Satisfactory</option><option value="monitor">⚠️ Monitor</option><option value="fail">❌ Attention Required</option><option value="na">N/A</option></select></div>
        <div class="fg"><label>Water Pressure</label><select id="i-pres"><option value="pass">✅ Satisfactory</option><option value="monitor">⚠️ Monitor</option><option value="fail">❌ Attention Required</option><option value="na">N/A</option></select></div>
        <div class="fg"><label>Taps & Fixtures</label><select id="i-taps"><option value="pass">✅ Satisfactory</option><option value="monitor">⚠️ Monitor</option><option value="fail">❌ Attention Required</option><option value="na">N/A</option></select></div>
        <div class="fg"><label>Toilets & Cisterns</label><select id="i-toil"><option value="pass">✅ Satisfactory</option><option value="monitor">⚠️ Monitor</option><option value="fail">❌ Attention Required</option><option value="na">N/A</option></select></div>
        <div class="fg"><label>Drainage</label><select id="i-drain"><option value="pass">✅ Satisfactory</option><option value="monitor">⚠️ Monitor</option><option value="fail">❌ Attention Required</option><option value="na">N/A</option></select></div>
        <div class="fg"><label>Pipe Condition (visible)</label><select id="i-pipes"><option value="pass">✅ Satisfactory</option><option value="monitor">⚠️ Monitor</option><option value="fail">❌ Attention Required</option><option value="na">N/A</option></select></div>
        <div class="fg"><label>Laundry / Washing Machine</label><select id="i-laund"><option value="pass">✅ Satisfactory</option><option value="monitor">⚠️ Monitor</option><option value="fail">❌ Attention Required</option><option value="na">N/A</option></select></div>
        <div class="fg"><label>Roof / Guttering / Stormwater</label><select id="i-roof"><option value="pass">✅ Satisfactory</option><option value="monitor">⚠️ Monitor</option><option value="fail">❌ Attention Required</option><option value="na">N/A</option></select></div>
        <div class="fsec">Gas Assessment</div>
        <div class="fg"><label>Gas Meter & Main Shutoff</label><select id="i-gmeter"><option value="pass">✅ Satisfactory</option><option value="monitor">⚠️ Monitor</option><option value="fail">❌ Attention Required</option><option value="na">N/A — No Gas</option></select></div>
        <div class="fg"><label>Gas Appliances</label><select id="i-gapps"><option value="pass">✅ Satisfactory</option><option value="monitor">⚠️ Monitor</option><option value="fail">❌ Attention Required</option><option value="na">N/A</option></select></div>
        <div class="fg"><label>Gas Pipework (visible)</label><select id="i-gpipes"><option value="pass">✅ Satisfactory</option><option value="monitor">⚠️ Monitor</option><option value="fail">❌ Attention Required</option><option value="na">N/A</option></select></div>
        <div class="fg"><label>Leak Test Result</label><select id="i-leak"><option value="pass">✅ No Leak Detected</option><option value="fail">❌ Leak Detected</option><option value="na">N/A — No Gas</option></select></div>
        <div class="fsec">🚨 Urgent Items & Defects</div>
        <div class="fg full"><label>Urgent Work Required (Safety / Compliance)</label><textarea id="i-urgent" rows="3" placeholder="List any items that require immediate attention — safety hazards, non-compliant installations, active leaks, gas defects…"></textarea></div>
        <div class="fg full"><label>Recommended Work (Non-Urgent)</label><textarea id="i-recommended" rows="3" placeholder="Items not urgent but recommended — aging HWS, worn washers, slow drainage, items to monitor at next inspection…"></textarea></div>
        <div class="fsec">Ratings & Summary</div>
        <div class="fg"><label>Plumbing Rating (1–10)</label><input id="i-prat" type="number" min="1" max="10" placeholder="8"></div>
        <div class="fg"><label>Gas Rating (1–10)</label><input id="i-grat" type="number" min="1" max="10" placeholder="9"></div>
        <div class="fg full"><label>Inspector's Summary Notes</label><textarea id="i-notes" rows="4" placeholder="Overall summary of the property's plumbing and gas condition, general remarks, anything the agent/landlord should be aware of…"></textarea></div>
      </div>
    </div>
    <div class="modal-foot">
      <button class="btn btn-secondary" onclick="close('m-insp')">Cancel</button>
      <button class="btn btn-green" onclick="saveInspection()">Generate Report</button>
    </div>
    <div class="modal-sticky-close"><button onclick="closeModal('m-insp')">✕ Close</button></div>
  </div>
</div>

<!-- ═══ MODAL: REPORT PREVIEW ═══ -->
<div class="overlay" id="m-report">
  <div class="modal modal-xl">
    <div class="modal-head">
      <div class="modal-title">Inspection Report</div>
      <div style="display:flex;gap:8px;align-items:center">
        <button class="btn btn-secondary btn-sm" onclick="printReport()">🖨 Print / PDF</button>
        <button class="modal-close" onclick="close('m-report')">✕</button>
      </div>
    </div>
    <div class="modal-body scrollable" id="report-body" style="background:#e8f0f5;padding:16px;-webkit-overflow-scrolling:touch"></div>
  </div>
</div>

<!-- ═══ MODAL: PM CHECKLIST ═══ -->
<div class="overlay" id="m-pm">
  <div class="modal modal-xl">
    <div class="modal-head"><div class="modal-title">Preventative Maintenance Checklist</div><button class="modal-close" onclick="close('m-pm')">✕</button></div>
    <div class="modal-body scrollable" style="max-height:75vh;-webkit-overflow-scrolling:touch">
      <div class="grid2" style="margin-bottom:14px">
        <div class="fg"><label>Client *</label><select id="pmf-client"></select></div>
        <div class="fg"><label>Date *</label><input id="pmf-date" type="date"></div>
        <div class="fg"><label>Technician</label><input id="pmf-tech" placeholder="Name / licence no."></div>
        <div class="fg"><label>Next PM Due (approx)</label><input id="pmf-next" type="date"></div>
        <div class="fg full"><label>Overall Notes</label><textarea id="pmf-notes" placeholder="Summary, observations, follow-up items…" rows="2"></textarea></div>
      </div>
      <div id="pmf-checklist"></div>
    </div>
    <div class="modal-foot space">
      <div style="display:flex;align-items:center;gap:12px">
        <div class="pm-progress" style="width:160px;margin-bottom:0"><div class="pm-progress-bar" id="pm-prog-bar"></div></div>
        <span id="pm-prog-label" style="font-size:12px;color:var(--text3);font-family:var(--mono)"></span>
      </div>
      <div style="display:flex;gap:8px">
        <button class="btn btn-secondary" onclick="close('m-pm')">Cancel</button>
        <button class="btn btn-primary" onclick="savePM()">Save PM Record</button>
      </div>
    <div class="modal-sticky-close"><button onclick="closeModal('m-report')">✕ Close</button></div>
    </div>
  </div>
</div>

<!-- ═══ MODAL: PHOTO LIGHTBOX ═══ -->
<div class="overlay" id="m-photo" style="align-items:center;justify-content:center;z-index:600">
  <div style="max-width:90vw;max-height:90vh;position:relative">
    <img id="lightbox-img" src="" style="max-width:90vw;max-height:85vh;border-radius:8px;display:block;box-shadow:0 8px 40px rgba(0,0,0,.8)">
    <button onclick="close('m-photo')" style="position:absolute;top:-12px;right:-12px;background:var(--surface);border:1px solid var(--border);border-radius:50%;width:32px;height:32px;display:flex;align-items:center;justify-content:center;cursor:pointer;font-size:16px;color:var(--text)">✕</button>
    <div id="lightbox-label" style="text-align:center;color:var(--text2);font-size:12px;margin-top:8px;font-family:'JetBrains Mono',monospace"></div>
  </div>
</div>

<script>
// ─────────────────────────────────────────
// DATA LAYER
// ─────────────────────────────────────────
const DB = {
  _k: 'plumbinc_ft',
  load() { try{return JSON.parse(localStorage.getItem(this._k)||'{}')}catch{return{}} },
  save(d) { try{localStorage.setItem(this._k,JSON.stringify(d))}catch(e){} },
  get() {
    const d=this.load();
    return {
      clients: d.clients||[],
      jobs: d.jobs||[],
      partners: d.partners||[],
      inspections: d.inspections||[],
      pmRecords: d.pmRecords||[]
    };
  },
  set(key,val) { const d=this.load(); d[key]=val; this.save(d); }
};

let currentId = null;
let pendingClientId = null;
let stageBefore = []; // {data, name}
let stageAfter  = [];

// ─────────────────────────────────────────
// UTILS
// ─────────────────────────────────────────
const uid = () => Date.now().toString(36)+Math.random().toString(36).slice(2,5);
const jobId = () => 'JOB-'+uid().toUpperCase().slice(0,8);
const today = () => new Date().toISOString().split('T')[0];
const fDate = s => { if(!s)return'—'; const d=new Date(s+'T00:00:00'); return d.toLocaleDateString('en-AU',{day:'2-digit',month:'short',year:'numeric'}); };
const scoreClass = n => n>=8?'green-score':n>=6?'amber-score':'red-score';
const badgeClass = n => n>=8?'b-green':n>=6?'b-amber':'b-red';
const fiIcon = v => v==='pass'?'✅':v==='monitor'?'⚠️':v==='fail'?'❌':'➖';
const fiLabel = v => v==='pass'?'Satisfactory':v==='monitor'?'Requires Monitoring':v==='fail'?'Requires Attention':'N/A';

function statusBadge(s) {
  if(s==='completed') return '<span class="badge b-green"><span class="dot"></span>Completed</span>';
  if(s==='in-progress') return '<span class="badge b-amber"><span class="dot"></span>In Progress</span>';
  if(s==='follow-up') return '<span class="badge b-red"><span class="dot"></span>Follow-Up</span>';
  return '<span class="badge b-grey">'+s+'</span>';
}

function pmStatusBadge(clientId) {
  const d=DB.get();
  const last=d.pmRecords.filter(p=>p.clientId===clientId).sort((a,b)=>new Date(b.date)-new Date(a.date))[0];
  const days=last?(new Date()-new Date(last.date))/(864e5):9999;
  if(days<90) return '<span class="badge b-green">✅ Up to Date</span>';
  if(days<180) return '<span class="badge b-amber">⚠️ Due Soon</span>';
  return '<span class="badge b-red">❌ Overdue</span>';
}

// ─────────────────────────────────────────
// NAV
// ─────────────────────────────────────────
const PAGE_LABEL = {dashboard:'Dashboard',clients:'Client Database',jobs:'Job History',photos:'Photo Folders',partners:'RE Partners',inspections:'Inspections',pm:'PM Checklists',calendar:'Job Calendar'};
const PAGE_ACTION = {clients:'＋ Add Client',jobs:'＋ Log Job',partners:'＋ Add Partner',inspections:'＋ New Inspection',pm:'＋ New PM Check'};

function nav(el) {
  document.querySelectorAll('.nav-item').forEach(n=>n.classList.remove('active'));
  document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));
  el.classList.add('active');
  const page=el.dataset.page;
  document.getElementById('page-'+page).classList.add('active');
  document.getElementById('page-title').textContent=PAGE_LABEL[page];
  const btn=document.getElementById('topbar-btn');
  if(PAGE_ACTION[page]){btn.style.display='';btn.textContent=PAGE_ACTION[page];}
  else btn.style.display='none';
  renderPage(page);
}

function topbarAction() {
  const p=document.querySelector('.nav-item.active')?.dataset.page;
  if(p==='clients') openAddClient();
  else if(p==='jobs') openAddJob();
  else if(p==='partners') openAddPartner();
  else if(p==='inspections') openNewInspection();
  else if(p==='pm') openNewPM();
}

function renderPage(p) {
  if(p==='dashboard') renderDashboard();
  else if(p==='clients') renderClients();
  else if(p==='jobs') renderJobs();
  else if(p==='photos') renderPhotoFolders();
  else if(p==='partners') renderPartners();
  else if(p==='inspections') renderInspections();
  else if(p==='pm') renderPMList();
  else if(p==='calendar') renderCalendar();
  updateCounts();
}

function updateCounts() {
  const d=DB.get();
  document.getElementById('nb-clients').textContent=d.clients.length;
  document.getElementById('nb-insp').textContent=d.inspections.length;
  document.getElementById('s-clients').textContent=d.clients.length;
  document.getElementById('s-jobs').textContent=d.jobs.length;
  document.getElementById('s-insp').textContent=d.inspections.length;
  const ov=d.clients.filter(c=>{
    const last=d.pmRecords.filter(p=>p.clientId===c.id).sort((a,b)=>new Date(b.date)-new Date(a.date))[0];
    return !last||(new Date()-new Date(last.date))/(864e5)>180;
  }).length;
  document.getElementById('s-pm').textContent=ov;
}

// ─────────────────────────────────────────
// MODAL HELPERS
// ─────────────────────────────────────────
function openModal(id){
  const el=document.getElementById(id);
  if(!el) return;
  el.classList.add('open');
  // Prevent body scroll on iOS
  document.body.style.overflow='hidden';
}

function closeModal(id){
  const el=document.getElementById(id);
  if(!el) return;
  el.classList.remove('open');
  // Re-enable body scroll if no other modals open
  if(!document.querySelector('.overlay.open')){
    document.body.style.overflow='';
  }
}

// Alias for legacy calls — point old open/close names to new ones
function open(id){ openModal(id); }
function close(id){ closeModal(id); }

document.querySelectorAll('.overlay').forEach(o=>{
  // Works on both mouse and touch
  o.addEventListener('click',e=>{ if(e.target===o){ closeModal(o.id); } });
  o.addEventListener('touchend',e=>{ if(e.target===o){ e.preventDefault(); closeModal(o.id); } });
});

// Escape key closes topmost open modal
document.addEventListener('keydown',e=>{
  if(e.key==='Escape'){
    const open=document.querySelector('.overlay.open');
    if(open) closeModal(open.id);
  }
});

function tab(el,paneId) {
  const modal=el.closest('.modal')||el.closest('.app');
  if(modal){
    modal.querySelectorAll('.tab').forEach(t=>t.classList.remove('active'));
    modal.querySelectorAll('.pane').forEach(p=>p.classList.remove('active'));
  }
  el.classList.add('active');
  const paneEl=document.getElementById(paneId);
  if(paneEl) paneEl.classList.add('active');
  if(paneId==='mt-tab-activity') renderActivityLog();
}

function popClientSelect(selId, selectedId) {
  const sel=document.getElementById(selId);
  const clients=DB.get().clients;
  sel.innerHTML='';
  if(!clients.length){sel.innerHTML='<option>Add a client first</option>';return;}
  clients.forEach(c=>{
    const o=document.createElement('option');
    o.value=c.id; o.textContent=`${c.fname} ${c.lname} — ${c.address}`;
    if(selectedId&&c.id===selectedId) o.selected=true;
    sel.appendChild(o);
  });
}

function popAgencySelect(selId) {
  const sel=document.getElementById(selId);
  const partners=DB.get().partners;
  sel.innerHTML='<option value="">-- Select Agency --</option>';
  partners.forEach(p=>{
    const o=document.createElement('option');
    o.value=p.id; o.textContent=p.agency;
    sel.appendChild(o);
  });
}

// ─────────────────────────────────────────
// LIVE SEARCH
// ─────────────────────────────────────────
function liveSearch(q) {
  const dd=document.getElementById('search-dropdown');
  if(!q.trim()){dd.classList.remove('open');dd.innerHTML='';return;}
  const clients=DB.get().clients.filter(c=>{
    const name=(c.fname+' '+c.lname).toLowerCase();
    const addr=(c.address||'').toLowerCase();
    const phone=(c.phone||'').toLowerCase();
    const qq=q.toLowerCase();
    return name.includes(qq)||addr.includes(qq)||phone.includes(qq);
  });
  if(!clients.length){
    dd.innerHTML='<div style="padding:12px 14px;color:var(--text3);font-size:13px">No clients found</div>';
    dd.classList.add('open');
    return;
  }
  dd.innerHTML=clients.map(c=>`
    <div class="search-result-item" onclick="openClientDetail('${c.id}');closeSearch()">
      <div class="sr-name">${c.fname} ${c.lname}</div>
      <div class="sr-addr">${c.address} · ${c.phone||'no phone'}</div>
    </div>
  `).join('');
  dd.classList.add('open');
}

function closeSearch(){
  document.getElementById('search-dropdown').classList.remove('open');
  document.getElementById('search-input').value='';
}

document.addEventListener('click',e=>{
  if(!e.target.closest('.search-wrap')) closeSearch();
});

// ─────────────────────────────────────────
// CLIENTS
// ─────────────────────────────────────────
function openAddClient() {
  document.getElementById('m-client-title').textContent='New Client';
  ['cf-fname','cf-lname','cf-phone','cf-email','cf-address','cf-ref','cf-year','cf-notes'].forEach(id=>document.getElementById(id).value='');
  document.getElementById('cf-type').value='residential';
  document.getElementById('cf-proptype').value='House';
  currentId=null;
  open('m-client');
}

function editCurrentClient() {
  const c=DB.get().clients.find(x=>x.id===currentId);
  if(!c) return;
  close('m-detail');
  document.getElementById('m-client-title').textContent='Edit Client';
  document.getElementById('cf-fname').value=c.fname||'';
  document.getElementById('cf-lname').value=c.lname||'';
  document.getElementById('cf-phone').value=c.phone||'';
  document.getElementById('cf-email').value=c.email||'';
  document.getElementById('cf-address').value=c.address||'';
  document.getElementById('cf-ref').value=c.referred||'';
  document.getElementById('cf-year').value=c.yearbuilt||'';
  document.getElementById('cf-notes').value=c.notes||'';
  document.getElementById('cf-type').value=c.type||'residential';
  document.getElementById('cf-proptype').value=c.proptype||'House';
  open('m-client');
}

function saveClient() {
  const fname=document.getElementById('cf-fname').value.trim();
  const lname=document.getElementById('cf-lname').value.trim();
  const address=document.getElementById('cf-address').value.trim();
  const phone=document.getElementById('cf-phone').value.trim();
  if(!fname||!lname||!address){alert('First name, last name and address are required.');return;}
  const d=DB.get();
  const data={fname,lname,address,phone,
    email:document.getElementById('cf-email').value.trim(),
    type:document.getElementById('cf-type').value,
    referred:document.getElementById('cf-ref').value.trim(),
    proptype:document.getElementById('cf-proptype').value,
    yearbuilt:document.getElementById('cf-year').value.trim(),
    notes:document.getElementById('cf-notes').value.trim()
  };
  if(currentId) {
    // EDIT mode
    const idx=d.clients.findIndex(c=>c.id===currentId);
    if(idx>-1){ d.clients[idx]={...d.clients[idx],...data}; }
    DB.set('clients',d.clients);
    close('m-client');
    setTimeout(()=>openClientDetail(currentId),100);
  } else {
    // NEW
    d.clients.push({id:uid(),createdAt:today(),author:stamp(),...data});
    DB.set('clients',d.clients);
    close('m-client');
    renderClients();
    logActivity('added a client', data.fname+' '+data.lname+' — '+data.address);
  }
  updateCounts();
}

function renderClients() {
  const d=DB.get();
  const tbody=document.getElementById('clients-tbody');
  if(!d.clients.length){
    tbody.innerHTML='<tr><td colspan="7"><div class="empty-state"><div class="empty-icon">👤</div>No clients yet. Click "+ Add Client".</div></td></tr>';
    return;
  }
  tbody.innerHTML=d.clients.map(c=>{
    const jobs=d.jobs.filter(j=>j.clientId===c.id).sort((a,b)=>new Date(b.date)-new Date(a.date));
    const last=jobs[0];
    const issue=jobs.find(j=>j.status==='follow-up');
    return `<tr onclick="openClientDetail('${c.id}')">
      <td><div class="cl-name">${c.fname} ${c.lname}</div><div class="cl-sub">${c.address}</div></td>
      <td class="mono" style="font-size:12px;color:var(--text2)">${c.phone||'—'}</td>
      <td><span class="badge b-grey">${c.type||'residential'}</span></td>
      <td class="mono" style="font-size:12px">${last?fDate(last.date):'—'}</td>
      <td style="max-width:160px;font-size:12px;color:var(--text3);white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${issue?issue.desc.slice(0,50):'—'}</td>
      <td>${pmStatusBadge(c.id)}</td>
      <td><button class="btn btn-ghost btn-xs">View →</button></td>
    </tr>`;
  }).join('');
}

function openClientDetail(id) {
  const d=DB.get();
  const c=d.clients.find(x=>x.id===id);
  if(!c) return;
  currentId=id;
  document.getElementById('det-name').textContent=c.fname+' '+c.lname;
  document.getElementById('det-addr').textContent=c.address;
  document.getElementById('dv-phone').textContent=c.phone||'—';
  document.getElementById('dv-email').textContent=c.email||'—';
  document.getElementById('dv-type').textContent=c.type||'—';
  document.getElementById('dv-pt').textContent=c.proptype||'—';
  document.getElementById('dv-yb').textContent=c.yearbuilt||'—';
  document.getElementById('dv-ref').textContent=c.referred||'—';
  document.getElementById('dv-notes').textContent=c.notes||'—';
  const jobs=d.jobs.filter(j=>j.clientId===id).sort((a,b)=>new Date(b.date)-new Date(a.date));
  const last=jobs[0];
  const issue=jobs.find(j=>j.desc);
  document.getElementById('dv-lv').textContent=last?fDate(last.date):'—';
  document.getElementById('dv-li').textContent=issue?(issue.type+': '+issue.desc.slice(0,60)):'—';
  document.getElementById('dv-tj').textContent=jobs.length;

  // Next-visit notes banner — show most recent job that has nextNotes
  const jobWithNotes=jobs.find(j=>j.nextNotes);
  const banner=document.getElementById('dv-nextnotes-banner');
  if(jobWithNotes){
    const prioBadge=jobWithNotes.followPrio==='urgent'?'🔴 URGENT':jobWithNotes.followPrio==='high'?'🟠 High Priority':jobWithNotes.followPrio==='normal'?'🟡 Normal':'🟢 Routine';
    banner.innerHTML=`<div style="margin-bottom:14px;padding:12px 14px;background:var(--blue-dim);border:1px solid rgba(88,166,255,.3);border-radius:8px">
      <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:5px">
        <span style="font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:var(--blue);font-family:'JetBrains Mono',monospace">📋 Next Visit Notes — from ${fDate(jobWithNotes.date)}</span>
        <span style="font-size:11px;color:var(--blue)">${prioBadge}${jobWithNotes.followDate?' · Due: '+fDate(jobWithNotes.followDate):''}</span>
      </div>
      <div style="font-size:13px;color:var(--text2)">${jobWithNotes.nextNotes}</div>
    </div>`;
  } else { banner.innerHTML=''; }

  // Urgent items banner — show most recent job with urgent note
  const jobWithUrgent=jobs.find(j=>j.urgent);
  const urgentBanner=document.getElementById('dv-urgent-banner');
  if(jobWithUrgent&&urgentBanner){
    urgentBanner.innerHTML=`<div style="margin-bottom:14px;padding:12px 14px;background:var(--red-dim);border:1px solid rgba(248,81,73,.3);border-radius:8px">
      <div style="font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:var(--red);margin-bottom:5px;font-family:'JetBrains Mono',monospace">🚨 Urgent / Outstanding Work — noted ${fDate(jobWithUrgent.date)}</div>
      <div style="font-size:13px;color:var(--text2)">${jobWithUrgent.urgent}</div>
    </div>`;
  } else if(urgentBanner){ urgentBanner.innerHTML=''; }

  // Jobs+Photos tab
  const jobList=document.getElementById('det-jobs-list');
  if(!jobs.length){
    jobList.innerHTML='<div class="empty-state"><div class="empty-icon">🔧</div>No jobs logged yet.</div>';
  } else {
    jobList.innerHTML=jobs.map(j=>renderJobCard(j,true)).join('');
  }

  // PM tab
  const pmRecs=d.pmRecords.filter(p=>p.clientId===id).sort((a,b)=>new Date(b.date)-new Date(a.date));
  const pmList=document.getElementById('det-pm-list');
  if(!pmRecs.length){
    pmList.innerHTML='<div class="empty-state"><div class="empty-icon">✅</div>No PM records.<br><a href="#" onclick="openNewPMForClient()" style="color:var(--copper)">Create one</a></div>';
  } else {
    pmList.innerHTML=pmRecs.map(pm=>`
      <div class="job-card" style="margin-bottom:10px">
        <div class="jc-header">
          <div><div class="jc-type">PM Check — ${fDate(pm.date)}</div>${pm.tech?`<div class="jc-desc" style="margin-top:2px">👷 ${pm.tech}</div>`:''}</div>
          <div>${pm.nextDue?`<span class="badge b-blue">Next: ${fDate(pm.nextDue)}</span>`:''}</div>
        </div>
        ${pm.notes?`<div class="jc-desc" style="margin-bottom:8px">📝 ${pm.notes}</div>`:''}
        <div style="display:flex;flex-wrap:wrap;gap:5px">
          ${pm.checks.filter(ch=>ch.checked).map(ch=>`<span class="badge b-green" style="font-size:10px">✅ ${ch.label}</span>`).join('')}
          ${pm.checks.filter(ch=>!ch.checked).map(ch=>`<span class="badge b-red" style="font-size:10px">❌ ${ch.label}</span>`).join('')}
        </div>
      </div>
    `).join('');
  }

  // Inspections tab
  const inspList=document.getElementById('det-insp-list');
  const inspections=d.inspections.filter(i=>i.clientId===id||i.address===c.address);
  if(!inspections.length){
    inspList.innerHTML='<div class="empty-state"><div class="empty-icon">📋</div>No inspections on file.</div>';
  } else {
    inspList.innerHTML=inspections.map(ins=>`
      <div class="activity-item" onclick="viewReport('${ins.id}')">
        <div class="ai-icon">📋</div>
        <div class="ai-body"><div class="ai-title">${ins.address}</div><div class="ai-sub">${ins.agency||'Independent'} · ${ins.inspector||''}</div></div>
        <div><span class="badge ${badgeClass(Math.round(((+ins.prat)+(+ins.grat))/2))}">${Math.round(((+ins.prat)+(+ins.grat))/2)}/10</span></div>
        <div class="ai-time">${fDate(ins.date)}</div>
      </div>
    `).join('');
  }

  // Reset tabs
  document.querySelectorAll('#m-detail .tab').forEach(t=>t.classList.remove('active'));
  document.querySelectorAll('#m-detail .pane').forEach(p=>p.classList.remove('active'));
  document.querySelector('#m-detail .tab').classList.add('active');
  document.getElementById('tp-overview').classList.add('active');
  open('m-detail');
}

function deleteClient(){
  if(!confirm('Delete this client and all records?')) return;
  const d=DB.get();
  DB.set('clients',d.clients.filter(c=>c.id!==currentId));
  DB.set('jobs',d.jobs.filter(j=>j.clientId!==currentId));
  DB.set('pmRecords',d.pmRecords.filter(p=>p.clientId!==currentId));
  close('m-detail');
  renderClients();
  renderDashboard();
  updateCounts();
}

// ─────────────────────────────────────────
// JOBS
// ─────────────────────────────────────────
function openAddJob(clientId) {
  pendingClientId=clientId||null;
  stageBefore=[]; stageAfter=[];
  document.getElementById('prev-before').innerHTML='';
  document.getElementById('prev-after').innerHTML='';
  document.getElementById('jf-date').value=today();
  document.getElementById('jf-desc').value='';
  document.getElementById('jf-tech').value='';
  document.getElementById('jf-licence').value='';
  document.getElementById('jf-arrive').value='';
  document.getElementById('jf-finish').value='';
  document.getElementById('jf-urgent').value='';
  document.getElementById('jf-findings').value='';
  document.getElementById('jf-nextnotes').value='';
  document.getElementById('jf-followdate').value='';
  document.getElementById('jf-followprio').value='';
  document.getElementById('jf-type').value='Service Call';
  document.getElementById('jf-status').value='completed';
  popClientSelect('jf-client',clientId);
  open('m-job');
  setTimeout(prefillTechName, 50);
}

function handlePhotos(input, side) {
  const container=document.getElementById('prev-'+(side==='before'?'before':'after'));
  const arr=side==='before'?stageBefore:stageAfter;
  Array.from(input.files).forEach(file=>{
    const reader=new FileReader();
    reader.onload=e=>{
      const entry={data:e.target.result,name:file.name,side};
      arr.push(entry);
      const chip=document.createElement('div');
      chip.className='prev-chip';
      const idx=arr.length-1;
      chip.innerHTML=`<img src="${e.target.result}" alt="${file.name}"><button class="prev-remove" onclick="removePhoto('${side}',${idx},this.parentNode)">✕</button>`;
      container.appendChild(chip);
    };
    reader.readAsDataURL(file);
  });
}

function removePhoto(side, idx, el) {
  (side==='before'?stageBefore:stageAfter).splice(idx,1);
  el.remove();
}

function saveJob() {
  const clientId=document.getElementById('jf-client').value;
  const date=document.getElementById('jf-date').value;
  if(!clientId||!date){alert('Select a client and date.');return;}
  const d=DB.get();
  const jid=jobId();
  const job={
    id:uid(), jobId:jid, clientId, date,
    type:document.getElementById('jf-type').value,
    desc:document.getElementById('jf-desc').value.trim(),
    status:document.getElementById('jf-status').value,
    tech:document.getElementById('jf-tech').value.trim(),
    licence:document.getElementById('jf-licence').value.trim(),
    arriveTime:document.getElementById('jf-arrive').value,
    finishTime:document.getElementById('jf-finish').value,
    urgent:document.getElementById('jf-urgent').value.trim(),
    findings:document.getElementById('jf-findings').value.trim(),
    nextNotes:document.getElementById('jf-nextnotes').value.trim(),
    followDate:document.getElementById('jf-followdate').value,
    followPrio:document.getElementById('jf-followprio').value,
    photosBefore:[...stageBefore],
    photosAfter:[...stageAfter],
    createdAt:today(),
    author: stamp()
  };
  d.jobs.push(job);
  DB.set('jobs',d.jobs);
  logActivity('logged a job', job.type + (document.getElementById('jf-desc').value.trim() ? ' — ' + document.getElementById('jf-desc').value.trim().slice(0,60) : ''));
  DB.set('jobs',d.jobs);
  stageBefore=[]; stageAfter=[];
  close('m-job');
  renderJobs();
  renderPhotoFolders();
  renderDashboard();
  updateCounts();
  if(pendingClientId) setTimeout(()=>openClientDetail(pendingClientId),150);
}

function renderJobCard(j, inDetail) {
  const totalPhotos=(j.photosBefore?.length||0)+(j.photosAfter?.length||0);
  const prioBadge = j.followPrio==='urgent'?'<span class="badge b-red">🔴 Urgent</span>':j.followPrio==='high'?'<span class="badge b-amber">🟠 High</span>':j.followPrio==='normal'?'<span class="badge b-blue">🟡 Normal</span>':j.followPrio==='low'?'<span class="badge b-green">🟢 Routine</span>':'';
  return `<div class="job-card">
    <div class="jc-header">
      <div>
        <div style="display:flex;align-items:center;gap:8px;flex-wrap:wrap">
          <div class="jc-type">${j.type}</div>
          <span class="mono" style="font-size:10px;color:var(--copper);background:var(--copper-dim);padding:2px 7px;border-radius:4px">${j.jobId||j.id.slice(0,8).toUpperCase()}</span>
          ${prioBadge}
        </div>
        ${j.tech?`<div style="font-size:12px;color:var(--text3);margin-top:3px">👷 ${j.tech}${j.licence?' · Lic: '+j.licence:''}</div>`:''}
        ${(j.arriveTime||j.finishTime)?`<div style="font-size:11px;color:var(--text3);margin-top:2px;font-family:'JetBrains Mono',monospace">⏱ ${j.arriveTime||'?'} → ${j.finishTime||'?'}</div>`:''}
      </div>
      <div style="text-align:right;flex-shrink:0">
        <div class="jc-date">${fDate(j.date)}</div>
        <div style="margin-top:4px">${statusBadge(j.status)}</div>
        ${totalPhotos?`<div style="margin-top:4px"><span class="badge b-blue">📁 ${totalPhotos} photo${totalPhotos>1?'s':''}</span></div>`:''}
      </div>
    </div>
    ${j.desc?`<div class="jc-desc" style="margin-top:6px;padding-top:6px;border-top:1px solid var(--border2)">${j.desc}</div>`:''}
    ${j.author?`<div style="margin-top:8px;display:flex;align-items:center;gap:8px">
      <div style="width:3px;height:20px;background:${j.author.techColour||'#888'};border-radius:2px;flex-shrink:0"></div>
      <span style="font-size:10px;font-weight:700;color:${j.author.techColour||'#888'}">${j.author.techName}</span>
      <span style="font-size:10px;color:var(--text3)">${j.author.at?'· '+new Date(j.author.at).toLocaleDateString('en-AU',{day:'2-digit',month:'short',year:'numeric'}):''}</span>
    </div>`:''}
    ${j.urgent?`<div style="margin-top:10px;padding:10px 12px;background:var(--red-dim);border:1px solid rgba(248,81,73,.25);border-radius:6px">
      <div style="font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:var(--red);margin-bottom:4px;font-family:'JetBrains Mono',monospace">🚨 Urgent / Recommended Work</div>
      <div style="font-size:13px;color:var(--text2);line-height:1.5">${j.urgent}</div>
    </div>`:''}
    ${j.findings?`<div style="margin-top:8px;padding:10px 12px;background:var(--amber-dim);border:1px solid rgba(210,153,34,.25);border-radius:6px">
      <div style="font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:var(--amber);margin-bottom:4px;font-family:'JetBrains Mono',monospace">🔍 Site Observations / Findings</div>
      <div style="font-size:13px;color:var(--text2);line-height:1.5">${j.findings}</div>
    </div>`:''}
    ${j.nextNotes?`<div style="margin-top:8px;padding:10px 12px;background:var(--blue-dim);border:1px solid rgba(88,166,255,.25);border-radius:6px">
      <div style="font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:var(--blue);margin-bottom:4px;font-family:'JetBrains Mono',monospace">📋 Next Visit Notes${j.followDate?' — Due: '+fDate(j.followDate):''}</div>
      <div style="font-size:13px;color:var(--text2);line-height:1.5">${j.nextNotes}</div>
    </div>`:''}
    ${renderJobPhotos(j)}
  </div>`;
}

function renderJobPhotos(j) {
  const before=j.photosBefore||[];
  const after=j.photosAfter||[];
  if(!before.length&&!after.length) return '';
  let html='<div class="photo-folder" style="margin-top:10px"><div class="photo-folder-header"><span>📁</span><span class="pf-job-id">'+(j.jobId||j.id.slice(0,8).toUpperCase())+'</span><span class="pf-job-desc">'+j.type+' · '+fDate(j.date)+'</span></div>';
  if(before.length){
    html+='<div class="photo-section-label">Before</div><div class="photo-grid">';
    html+=before.map((p,i)=>`<div class="photo-slot" onclick="lightbox('${p.data||p}','Before — ${j.type} ${fDate(j.date)}')"><img src="${p.data||p}" alt="before"><div class="slot-label">Before</div></div>`).join('');
    html+='</div>';
  }
  if(after.length){
    html+='<div class="photo-section-label">After</div><div class="photo-grid">';
    html+=after.map((p,i)=>`<div class="photo-slot" onclick="lightbox('${p.data||p}','After — ${j.type} ${fDate(j.date)}')"><img src="${p.data||p}" alt="after"><div class="slot-label">After</div></div>`).join('');
    html+='</div>';
  }
  html+='</div>';
  return html;
}

function lightbox(src, label) {
  document.getElementById('lightbox-img').src=src;
  document.getElementById('lightbox-label').textContent=label;
  open('m-photo');
}

function renderJobs() {
  const d=DB.get();
  const jobs=[...d.jobs].sort((a,b)=>new Date(b.date)-new Date(a.date));
  const tbody=document.getElementById('jobs-tbody');
  if(!jobs.length){tbody.innerHTML='<tr><td colspan="8"><div class="empty-state">No jobs yet.</div></td></tr>';return;}
  tbody.innerHTML=jobs.map(j=>{
    const c=d.clients.find(x=>x.id===j.clientId);
    const np=(j.photosBefore?.length||0)+(j.photosAfter?.length||0);
    return `<tr onclick="openClientDetail('${j.clientId}')">
      <td class="mono" style="font-size:11px;color:var(--copper)">${j.jobId||j.id.slice(0,8).toUpperCase()}</td>
      <td class="mono" style="font-size:12px">${fDate(j.date)}</td>
      <td><div class="cl-name">${c?c.fname+' '+c.lname:'—'}</div><div class="cl-sub">${c?c.address:''}</div></td>
      <td><span class="tag">${j.type}</span></td>
      <td style="max-width:180px;font-size:12px;color:var(--text2);white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${j.desc||'—'}</td>
      <td>${statusBadge(j.status)}</td>
      <td>${np?`<span class="badge b-blue">📁 ${np}</span>`:'<span class="muted" style="font-size:12px">—</span>'}</td>
      <td><button class="btn btn-ghost btn-xs">View →</button></td>
    </tr>`;
  }).join('');
}

// ─────────────────────────────────────────
// PHOTO FOLDERS PAGE
// ─────────────────────────────────────────
function renderPhotoFolders() {
  const d=DB.get();
  const container=document.getElementById('photo-folders-list');
  const jobs=[...d.jobs].filter(j=>(j.photosBefore?.length||0)+(j.photosAfter?.length||0)>0).sort((a,b)=>new Date(b.date)-new Date(a.date));
  if(!jobs.length){
    container.innerHTML='<div class="empty-state" style="margin-top:32px"><div class="empty-icon">📁</div>No photo folders yet.<br>Photos are automatically organised when you log a job.</div>';
    return;
  }
  container.innerHTML=jobs.map(j=>{
    const c=d.clients.find(x=>x.id===j.clientId);
    const total=(j.photosBefore?.length||0)+(j.photosAfter?.length||0);
    return `<div class="card" style="margin-bottom:14px;overflow:visible">
      <div style="padding:14px 16px;display:flex;align-items:center;gap:12px;border-bottom:1px solid var(--border2)">
        <div style="font-size:20px">📁</div>
        <div style="flex:1">
          <div style="font-weight:700;font-size:14px;color:var(--text);">${c?c.fname+' '+c.lname:'Unknown Client'}</div>
          <div style="font-size:12px;color:var(--text3)">${c?c.address:''} · ${j.type}</div>
        </div>
        <div style="text-align:right">
          <span class="mono" style="font-size:11px;color:var(--copper);background:var(--copper-dim);padding:3px 9px;border-radius:4px">${j.jobId||j.id.slice(0,8).toUpperCase()}</span>
          <div style="font-size:11px;color:var(--text3);margin-top:4px">${fDate(j.date)} · ${total} photo${total>1?'s':''}</div>
        </div>
      </div>
      ${renderJobPhotos(j)}
    </div>`;
  }).join('');
}

// ─────────────────────────────────────────
// PARTNERS
// ─────────────────────────────────────────
function openAddPartner() {
  ['pf-agency','pf-contact','pf-phone','pf-email','pf-notes'].forEach(id=>document.getElementById(id).value='');
  open('m-partner');
}

function savePartner() {
  const agency=document.getElementById('pf-agency').value.trim();
  if(!agency){alert('Agency name required.');return;}
  const d=DB.get();
  d.partners.push({id:uid(),agency,contact:document.getElementById('pf-contact').value.trim(),phone:document.getElementById('pf-phone').value.trim(),email:document.getElementById('pf-email').value.trim(),notes:document.getElementById('pf-notes').value.trim(),createdAt:today()});
  DB.set('partners',d.partners);
  close('m-partner');
  renderPartners();
}

function renderPartners() {
  const d=DB.get();
  const tbody=document.getElementById('partners-tbody');
  if(!d.partners.length){tbody.innerHTML='<tr><td colspan="6"><div class="empty-state">No RE partners yet.</div></td></tr>';return;}
  tbody.innerHTML=d.partners.map(p=>{
    const insp=d.inspections.filter(i=>i.partnerId===p.id).length;
    const ref=d.clients.filter(c=>c.referred===p.agency).length;
    return `<tr>
      <td><div class="cl-name">${p.agency}</div><div class="cl-sub">${p.contact||''}</div></td>
      <td style="font-size:12px;color:var(--text3)">${p.phone||'—'}<br>${p.email||''}</td>
      <td class="mono" style="text-align:center">${ref}</td>
      <td class="mono" style="text-align:center">${insp}</td>
      <td><span class="badge b-green">Active</span></td>
      <td><button class="btn btn-danger btn-xs" onclick="delPartner('${p.id}')">🗑</button></td>
    </tr>`;
  }).join('');
}

function delPartner(id){if(!confirm('Remove partner?'))return;const d=DB.get();DB.set('partners',d.partners.filter(p=>p.id!==id));renderPartners();}

// ─────────────────────────────────────────
// INSPECTIONS
// ─────────────────────────────────────────
function openNewInspection(clientId) {
  pendingClientId=clientId||null;
  document.getElementById('inf-date').value=today();
  ['inf-addr','inf-agent','inf-agentcontact','inf-insp','inf-licence','inf-arrive','inf-finish','inf-year','inf-conditions','i-notes','i-urgent','i-recommended'].forEach(id=>document.getElementById(id).value='');
  document.getElementById('inf-proptype').value='House';
  ['i-hws','i-pres','i-taps','i-toil','i-drain','i-pipes','i-laund','i-roof','i-gmeter','i-gapps','i-gpipes','i-leak'].forEach(id=>document.getElementById(id).value='pass');
  document.getElementById('i-prat').value='8';
  document.getElementById('i-grat').value='9';
  if(clientId){const c=DB.get().clients.find(x=>x.id===clientId);if(c){document.getElementById('inf-addr').value=c.address;document.getElementById('inf-proptype').value=c.proptype||'House';document.getElementById('inf-year').value=c.yearbuilt||'';}}
  popAgencySelect('inf-agency');
  open('m-insp');
}

function saveInspection() {
  const addr=document.getElementById('inf-addr').value.trim();
  const date=document.getElementById('inf-date').value;
  if(!addr||!date){alert('Address and date required.');return;}
  const d=DB.get();
  const pid=document.getElementById('inf-agency').value;
  const partner=d.partners.find(p=>p.id===pid);
  const prat=parseInt(document.getElementById('i-prat').value)||8;
  const grat=parseInt(document.getElementById('i-grat').value)||9;
  const ins={
    id:uid(), address:addr, date,
    partnerId:pid, agency:partner?partner.agency:'',
    agent:document.getElementById('inf-agent').value.trim(),
    agentContact:document.getElementById('inf-agentcontact').value.trim(),
    inspector:document.getElementById('inf-insp').value.trim(),
    licence:document.getElementById('inf-licence').value.trim(),
    arriveTime:document.getElementById('inf-arrive').value,
    finishTime:document.getElementById('inf-finish').value,
    proptype:document.getElementById('inf-proptype').value,
    yearBuilt:document.getElementById('inf-year').value.trim(),
    conditions:document.getElementById('inf-conditions').value.trim(),
    hws:document.getElementById('i-hws').value,
    pres:document.getElementById('i-pres').value,
    taps:document.getElementById('i-taps').value,
    toil:document.getElementById('i-toil').value,
    drain:document.getElementById('i-drain').value,
    pipes:document.getElementById('i-pipes').value,
    laund:document.getElementById('i-laund').value,
    roof:document.getElementById('i-roof').value,
    gmeter:document.getElementById('i-gmeter').value,
    gapps:document.getElementById('i-gapps').value,
    gpipes:document.getElementById('i-gpipes').value,
    leak:document.getElementById('i-leak').value,
    urgent:document.getElementById('i-urgent').value.trim(),
    recommended:document.getElementById('i-recommended').value.trim(),
    notes:document.getElementById('i-notes').value.trim(),
    prat, grat,
    clientId:pendingClientId||'',
    createdAt:today(),
    author: stamp()
  };
  d.inspections.push(ins);
  DB.set('inspections',d.inspections);
  logActivity('created an inspection report', addr);
  close('m-insp');
  renderInspections();
  updateCounts();
  setTimeout(()=>viewReport(ins.id),150);
}

function renderInspections() {
  const d=DB.get();
  const tbody=document.getElementById('insp-tbody');
  const insp=[...d.inspections].sort((a,b)=>new Date(b.date)-new Date(a.date));
  if(!insp.length){tbody.innerHTML='<tr><td colspan="8"><div class="empty-state">No inspections yet.</div></td></tr>';return;}
  tbody.innerHTML=insp.map(ins=>{
    const overall=Math.round(((+ins.prat)+(+ins.grat))/2);
    const rn='FT-'+ins.id.slice(-6).toUpperCase();
    return `<tr onclick="viewReport('${ins.id}')" style="cursor:pointer">
      <td class="mono" style="font-size:11px;color:var(--copper)">${rn}</td>
      <td class="mono" style="font-size:12px">${fDate(ins.date)}</td>
      <td><div class="cl-name">${ins.address}</div></td>
      <td style="font-size:12px;color:var(--text3)">${ins.agency||'—'}</td>
      <td><span class="badge ${badgeClass(+ins.prat)}">${ins.prat}/10</span></td>
      <td><span class="badge ${badgeClass(+ins.grat)}">${ins.grat}/10</span></td>
      <td><span class="badge ${badgeClass(overall)}">${overall}/10</span></td>
      <td><button class="btn btn-ghost btn-xs">Report →</button></td>
    </tr>`;
  }).join('');
}

function viewReport(id) {
  const ins=DB.get().inspections.find(i=>i.id===id);
  if(!ins) return;
  const overall=Math.round(((+ins.prat)+(+ins.grat))/2);
  const rn='FT-'+ins.id.slice(-6).toUpperCase();
  const overallLabel=overall>=8?'Good Condition — No Immediate Action Required':overall>=6?'Acceptable — Items Require Monitoring':'Attention Required — Action Recommended';
  const overallColour=overall>=8?'#1a7a4a':overall>=6?'#c87a10':'#b83030';

  const plItems=[
    {k:'hws',l:'Hot Water System'},{k:'pres',l:'Water Pressure & PRV'},
    {k:'taps',l:'Taps & Fixtures'},{k:'toil',l:'Toilets & Cisterns'},
    {k:'drain',l:'Drainage & Waste Lines'},{k:'pipes',l:'Visible Pipe Condition'},
    {k:'laund',l:'Laundry / Washing Machine Connection'},{k:'roof',l:'Roof Plumbing / Guttering / Stormwater'},
  ];
  const gasItems=[
    {k:'gmeter',l:'Gas Meter & Emergency Shutoff'},{k:'gapps',l:'Gas Appliances'},
    {k:'gpipes',l:'Gas Pipework (Visible)'},{k:'leak',l:'Leak Test (Electronic & Soap)'},
  ];

  const countByStatus=(items,status)=>items.filter(i=>ins[i.k]===status).length;
  const plPass=countByStatus(plItems,'pass'), plMon=countByStatus(plItems,'monitor'), plFail=countByStatus(plItems,'fail');
  const gaPass=countByStatus(gasItems,'pass'), gaMon=countByStatus(gasItems,'monitor'), gaFail=countByStatus(gasItems,'fail');

  const fmtTime=t=>{ if(!t) return '—'; const [h,m]=t.split(':'); const hh=+h; return `${hh>12?hh-12:hh||12}:${m} ${hh>=12?'PM':'AM'}`; };
  const duration=(a,f)=>{ if(!a||!f) return ''; try{ const am=a.split(':'), fm=f.split(':'); const mins=(+fm[0]*60+ +fm[1])-(+am[0]*60+ +am[1]); if(mins<=0) return ''; const h=Math.floor(mins/60), m=mins%60; return ` (${h?h+'h ':''}${m?m+'min':''})`; }catch{return '';} };

  document.getElementById('report-body').innerHTML=`
  <div class="report-page" style="font-size:13px">

    <!-- HEADER -->
    <div style="display:flex;justify-content:space-between;align-items:flex-start;padding-bottom:20px;border-bottom:3px solid #0d1117;margin-bottom:20px">
      <div>
        <div style="font-family:'Barlow Condensed',sans-serif;font-size:32px;font-weight:800;letter-spacing:1.5px;line-height:1">PLUMB<span style="color:#e8821a">.INC</span></div>
        <div style="font-size:10px;letter-spacing:3px;color:#8b949e;margin-top:3px;font-family:'JetBrains Mono',monospace">LICENSED PLUMBING &amp; GAS SERVICES</div>
        <div style="font-size:11px;color:#4a6075;margin-top:8px;line-height:1.6">
          ABN: XX XXX XXX XXX &nbsp;|&nbsp; Plumbing Licence: PL XXXXX<br>
          Phone: (08) XXXX XXXX &nbsp;|&nbsp; admin@plumbinc.com.au
        </div>
      </div>
      <div style="text-align:right">
        <div style="background:#0d1117;color:white;padding:14px 20px;border-radius:8px;margin-bottom:8px">
          <div style="font-size:9px;letter-spacing:2px;text-transform:uppercase;color:#4a5568;font-family:'JetBrains Mono',monospace">Report No.</div>
          <div style="font-family:'JetBrains Mono',monospace;font-size:16px;font-weight:700;margin-top:2px;color:#f59840">${rn}</div>
        </div>
        <div style="font-size:11px;color:#8b949e;text-align:right;line-height:1.8">
          <strong style="color:#0d1117">Issued:</strong> ${fDate(ins.date)}<br>
          <strong style="color:#0d1117">Inspection Type:</strong> Complimentary
        </div>
      </div>
    </div>

    <!-- DOCUMENT TITLE -->
    <div style="text-align:center;margin-bottom:20px">
      <div style="font-family:'Barlow Condensed',sans-serif;font-size:22px;font-weight:800;letter-spacing:2px;text-transform:uppercase;color:#0d1117">Plumbing &amp; Gas Inspection Report</div>
      <div style="font-size:11px;color:#8b949e;letter-spacing:1px;margin-top:3px">Pre-Tenancy / Change of Occupancy — Complimentary Service</div>
    </div>

    <!-- PROPERTY + ASSIGNMENT BOX -->
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-bottom:18px">
      <div style="background:#f4f7f9;border:1px solid #c8d8e4;border-radius:8px;padding:14px 16px">
        <div style="font-size:10px;font-weight:700;letter-spacing:2px;text-transform:uppercase;color:#e8821a;border-bottom:1px solid #c8d8e4;padding-bottom:6px;margin-bottom:10px;font-family:'JetBrains Mono',monospace">Property Details</div>
        <table style="width:100%;font-size:12px;border-collapse:collapse">
          <tr><td style="color:#8b949e;padding:3px 0;width:110px;vertical-align:top">Address</td><td style="font-weight:700;color:#0d1117;padding:3px 0">${ins.address}</td></tr>
          <tr><td style="color:#8b949e;padding:3px 0">Property Type</td><td style="font-weight:700;color:#0d1117;padding:3px 0">${ins.proptype||'—'}</td></tr>
          <tr><td style="color:#8b949e;padding:3px 0">Year Built</td><td style="font-weight:700;color:#0d1117;padding:3px 0">${ins.yearBuilt||'—'}</td></tr>
          <tr><td style="color:#8b949e;padding:3px 0">Site Conditions</td><td style="font-weight:700;color:#0d1117;padding:3px 0">${ins.conditions||'—'}</td></tr>
        </table>
      </div>
      <div style="background:#f4f7f9;border:1px solid #c8d8e4;border-radius:8px;padding:14px 16px">
        <div style="font-size:10px;font-weight:700;letter-spacing:2px;text-transform:uppercase;color:#e8821a;border-bottom:1px solid #c8d8e4;padding-bottom:6px;margin-bottom:10px;font-family:'JetBrains Mono',monospace">Inspection Details</div>
        <table style="width:100%;font-size:12px;border-collapse:collapse">
          <tr><td style="color:#8b949e;padding:3px 0;width:110px">Inspector</td><td style="font-weight:700;color:#0d1117;padding:3px 0">${ins.inspector||'—'}</td></tr>
          <tr><td style="color:#8b949e;padding:3px 0">Licence No.</td><td style="font-weight:700;color:#0d1117;padding:3px 0">${ins.licence||'—'}</td></tr>
          <tr><td style="color:#8b949e;padding:3px 0">Arrival Time</td><td style="font-weight:700;color:#0d1117;padding:3px 0">${fmtTime(ins.arriveTime)}</td></tr>
          <tr><td style="color:#8b949e;padding:3px 0">Finish Time</td><td style="font-weight:700;color:#0d1117;padding:3px 0">${fmtTime(ins.finishTime)}${duration(ins.arriveTime,ins.finishTime)}</td></tr>
          <tr><td style="color:#8b949e;padding:3px 0">Report No.</td><td style="font-weight:700;font-family:'JetBrains Mono',monospace;color:#e8821a;padding:3px 0">${rn}</td></tr>
        </table>
      </div>
    </div>

    <!-- AGENT / AGENCY BOX -->
    <div style="background:#f4f7f9;border:1px solid #c8d8e4;border-radius:8px;padding:12px 16px;margin-bottom:18px;display:grid;grid-template-columns:1fr 1fr 1fr;gap:12px">
      <div>
        <div style="font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:#8b949e;margin-bottom:3px;font-family:'JetBrains Mono',monospace">Real Estate Agency</div>
        <div style="font-size:13px;font-weight:700;color:#0d1117">${ins.agency||'Independent'}</div>
      </div>
      <div>
        <div style="font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:#8b949e;margin-bottom:3px;font-family:'JetBrains Mono',monospace">Agent Name</div>
        <div style="font-size:13px;font-weight:700;color:#0d1117">${ins.agent||'—'}</div>
      </div>
      <div>
        <div style="font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:#8b949e;margin-bottom:3px;font-family:'JetBrains Mono',monospace">Agent Contact</div>
        <div style="font-size:13px;font-weight:700;color:#0d1117">${ins.agentContact||'—'}</div>
      </div>
    </div>

    <!-- OVERALL RESULT BANNER -->
    <div style="background:${overallColour};color:white;border-radius:8px;padding:16px 20px;margin-bottom:20px;display:flex;align-items:center;justify-content:space-between">
      <div>
        <div style="font-size:10px;letter-spacing:2px;text-transform:uppercase;opacity:.75;font-family:'JetBrains Mono',monospace">Overall Assessment</div>
        <div style="font-family:'Barlow Condensed',sans-serif;font-size:22px;font-weight:800;letter-spacing:1px;margin-top:2px">${overallLabel}</div>
      </div>
      <div style="text-align:right">
        <div style="font-family:'Barlow Condensed',sans-serif;font-size:52px;font-weight:800;line-height:1">${overall}<span style="font-size:24px">/10</span></div>
      </div>
    </div>

    <!-- SCORE CARDS -->
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-bottom:20px">
      <div style="border:2px solid ${scoreClass(+ins.prat)==='green-score'?'#1a7a4a':scoreClass(+ins.prat)==='amber-score'?'#c87a10':'#b83030'};border-radius:8px;padding:14px 18px;display:flex;justify-content:space-between;align-items:center">
        <div>
          <div style="font-size:10px;font-weight:700;letter-spacing:2px;text-transform:uppercase;color:#8b949e;font-family:'JetBrains Mono',monospace">Plumbing Score</div>
          <div style="font-size:12px;color:#4a6075;margin-top:4px">${plPass} pass · ${plMon} monitor · ${plFail} fail</div>
        </div>
        <div style="font-family:'Barlow Condensed',sans-serif;font-size:44px;font-weight:800;line-height:1;${scoreClass(+ins.prat)==='green-score'?'color:#1a7a4a':scoreClass(+ins.prat)==='amber-score'?'color:#c87a10':'color:#b83030'}">${ins.prat}/10</div>
      </div>
      <div style="border:2px solid ${scoreClass(+ins.grat)==='green-score'?'#1a7a4a':scoreClass(+ins.grat)==='amber-score'?'#c87a10':'#b83030'};border-radius:8px;padding:14px 18px;display:flex;justify-content:space-between;align-items:center">
        <div>
          <div style="font-size:10px;font-weight:700;letter-spacing:2px;text-transform:uppercase;color:#8b949e;font-family:'JetBrains Mono',monospace">Gas Score</div>
          <div style="font-size:12px;color:#4a6075;margin-top:4px">${gaPass} pass · ${gaMon} monitor · ${gaFail} fail</div>
        </div>
        <div style="font-family:'Barlow Condensed',sans-serif;font-size:44px;font-weight:800;line-height:1;${scoreClass(+ins.grat)==='green-score'?'color:#1a7a4a':scoreClass(+ins.grat)==='amber-score'?'color:#c87a10':'color:#b83030'}">${ins.grat}/10</div>
      </div>
    </div>

    ${(ins.urgent) ? `
    <!-- URGENT DEFECTS BOX -->
    <div style="border:2px solid #b83030;border-radius:8px;padding:14px 18px;margin-bottom:18px;background:#faeaea">
      <div style="font-family:'Barlow Condensed',sans-serif;font-size:15px;font-weight:800;letter-spacing:1.5px;text-transform:uppercase;color:#b83030;margin-bottom:8px">⚠ Urgent Items — Action Required</div>
      <div style="font-size:13px;color:#2a3a4a;line-height:1.7;white-space:pre-line">${ins.urgent}</div>
    </div>` : ''}

    ${(ins.recommended) ? `
    <!-- RECOMMENDED WORK BOX -->
    <div style="border:1px solid #f0d090;border-radius:8px;padding:14px 18px;margin-bottom:18px;background:#fdf8e6">
      <div style="font-family:'Barlow Condensed',sans-serif;font-size:15px;font-weight:800;letter-spacing:1.5px;text-transform:uppercase;color:#c87a10;margin-bottom:8px">Recommended Work (Non-Urgent)</div>
      <div style="font-size:13px;color:#2a3a4a;line-height:1.7;white-space:pre-line">${ins.recommended}</div>
    </div>` : ''}

    <!-- PLUMBING FINDINGS TABLE -->
    <div style="font-family:'Barlow Condensed',sans-serif;font-size:16px;font-weight:800;letter-spacing:1.5px;text-transform:uppercase;color:#e8821a;border-bottom:2px solid #e8f0f5;padding-bottom:6px;margin:0 0 10px">Plumbing Assessment</div>
    <table style="width:100%;border-collapse:collapse;margin-bottom:18px;font-size:12.5px">
      <thead>
        <tr style="background:#f4f7f9">
          <th style="text-align:left;padding:8px 10px;border:1px solid #dce8f0;font-size:10px;letter-spacing:1px;text-transform:uppercase;color:#8b949e;font-family:'JetBrains Mono',monospace">Item Inspected</th>
          <th style="text-align:center;padding:8px 10px;border:1px solid #dce8f0;font-size:10px;letter-spacing:1px;text-transform:uppercase;color:#8b949e;font-family:'JetBrains Mono',monospace;width:140px">Result</th>
        </tr>
      </thead>
      <tbody>
        ${plItems.map((item,i)=>`
        <tr style="background:${i%2===0?'white':'#f9fbfc'}">
          <td style="padding:8px 10px;border:1px solid #dce8f0;color:#2a3a4a">${item.l}</td>
          <td style="padding:8px 10px;border:1px solid #dce8f0;text-align:center">
            <span style="display:inline-block;padding:3px 10px;border-radius:12px;font-size:11px;font-weight:700;
              background:${ins[item.k]==='pass'?'#e6f5ed':ins[item.k]==='monitor'?'#fdf4e0':ins[item.k]==='fail'?'#faeaea':'#f4f7f9'};
              color:${ins[item.k]==='pass'?'#1a7a4a':ins[item.k]==='monitor'?'#c87a10':ins[item.k]==='fail'?'#b83030':'#8b949e'}">
              ${fiIcon(ins[item.k])} ${fiLabel(ins[item.k])}
            </span>
          </td>
        </tr>`).join('')}
      </tbody>
    </table>

    <!-- GAS FINDINGS TABLE -->
    <div style="font-family:'Barlow Condensed',sans-serif;font-size:16px;font-weight:800;letter-spacing:1.5px;text-transform:uppercase;color:#e8821a;border-bottom:2px solid #e8f0f5;padding-bottom:6px;margin:0 0 10px">Gas Assessment</div>
    <table style="width:100%;border-collapse:collapse;margin-bottom:18px;font-size:12.5px">
      <thead>
        <tr style="background:#f4f7f9">
          <th style="text-align:left;padding:8px 10px;border:1px solid #dce8f0;font-size:10px;letter-spacing:1px;text-transform:uppercase;color:#8b949e;font-family:'JetBrains Mono',monospace">Item Inspected</th>
          <th style="text-align:center;padding:8px 10px;border:1px solid #dce8f0;font-size:10px;letter-spacing:1px;text-transform:uppercase;color:#8b949e;font-family:'JetBrains Mono',monospace;width:140px">Result</th>
        </tr>
      </thead>
      <tbody>
        ${gasItems.map((item,i)=>`
        <tr style="background:${i%2===0?'white':'#f9fbfc'}">
          <td style="padding:8px 10px;border:1px solid #dce8f0;color:#2a3a4a">${item.l}</td>
          <td style="padding:8px 10px;border:1px solid #dce8f0;text-align:center">
            <span style="display:inline-block;padding:3px 10px;border-radius:12px;font-size:11px;font-weight:700;
              background:${ins[item.k]==='pass'?'#e6f5ed':ins[item.k]==='monitor'?'#fdf4e0':ins[item.k]==='fail'?'#faeaea':'#f4f7f9'};
              color:${ins[item.k]==='pass'?'#1a7a4a':ins[item.k]==='monitor'?'#c87a10':ins[item.k]==='fail'?'#b83030':'#8b949e'}">
              ${fiIcon(ins[item.k])} ${fiLabel(ins[item.k])}
            </span>
          </td>
        </tr>`).join('')}
      </tbody>
    </table>

    ${ins.notes?`
    <!-- INSPECTOR NOTES -->
    <div style="font-family:'Barlow Condensed',sans-serif;font-size:16px;font-weight:800;letter-spacing:1.5px;text-transform:uppercase;color:#e8821a;border-bottom:2px solid #e8f0f5;padding-bottom:6px;margin:0 0 10px">Inspector's Summary</div>
    <div style="font-size:13px;color:#2a3a4a;line-height:1.8;background:#f4f7f9;padding:14px 16px;border-radius:8px;border:1px solid #c8d8e4;margin-bottom:18px;white-space:pre-line">${ins.notes}</div>`:''}

    <!-- DISCLAIMER -->
    <div style="font-size:11px;color:#8b949e;line-height:1.7;background:#f4f7f9;padding:12px 16px;border-radius:8px;border:1px solid #c8d8e4;margin-bottom:20px">
      <strong style="color:#4a6075">Disclaimer:</strong> This report is based on a visual inspection of accessible plumbing and gas components at the time of inspection and does not constitute a full structural or engineering assessment. Areas not accessible or outside scope have not been assessed. Items identified as requiring attention are recommendations only unless marked as urgent safety or compliance issues. This inspection is provided complimentary as part of the Plumb.Inc Real Estate Partner Program. Plumb.Inc accepts no liability for defects not visible at the time of inspection.
    </div>

    <!-- SIGNATURES -->
    <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:20px;padding-top:18px;border-top:1px solid #c8d8e4">
      <div>
        <div style="height:44px;border-bottom:1px solid #0d1117;margin-bottom:5px"></div>
        <div style="font-size:10px;color:#8b949e">Inspector Signature</div>
        <div style="font-size:11px;font-weight:700;color:#2a3a4a;margin-top:2px">${ins.inspector||'—'}</div>
        <div style="font-size:10px;color:#8b949e">Lic: ${ins.licence||'—'}</div>
      </div>
      <div>
        <div style="height:44px;border-bottom:1px solid #0d1117;margin-bottom:5px"></div>
        <div style="font-size:10px;color:#8b949e">Agent Acknowledgement</div>
        <div style="font-size:11px;font-weight:700;color:#2a3a4a;margin-top:2px">${ins.agent||'—'}</div>
        <div style="font-size:10px;color:#8b949e">${ins.agency||''}</div>
      </div>
      <div>
        <div style="height:44px;border-bottom:1px solid #0d1117;margin-bottom:5px"></div>
        <div style="font-size:10px;color:#8b949e">Date of Inspection</div>
        <div style="font-size:11px;font-weight:700;color:#2a3a4a;margin-top:2px">${fDate(ins.date)}</div>
        <div style="font-size:10px;color:#8b949e">${fmtTime(ins.arriveTime)} – ${fmtTime(ins.finishTime)}</div>
      </div>
    </div>

    <!-- FOOTER -->
    <div style="text-align:center;margin-top:22px;padding-top:14px;border-top:1px solid #e8f0f5;font-size:10px;color:#c8d8e4;font-family:'JetBrains Mono',monospace;letter-spacing:1px;text-transform:uppercase">
      Plumb.Inc FlowTrack Pro &nbsp;·&nbsp; Report ${rn} &nbsp;·&nbsp; ${fDate(ins.date)} &nbsp;·&nbsp; Confidential — Prepared for ${ins.agency||'Client'} &nbsp;·&nbsp; plumbinc.com.au
    </div>

  </div>`;
  open('m-report');
}

function printReport() {
  const c=document.getElementById('report-body').innerHTML;
  const w=window.open('','_blank');
  w.document.write(`<!DOCTYPE html><html><head><title>Inspection Report</title>
  <link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@400;600;700;800&family=Lato:wght@300;400;700&family=JetBrains+Mono&display=swap" rel="stylesheet">
  <style>*{box-sizing:border-box;margin:0;padding:0}body{font-family:'Lato',sans-serif;background:white;padding:0;font-size:13px}</style>
  </head><body>${c}</body></html>`);
  w.document.close();
  setTimeout(()=>w.print(),700);
}

// ─────────────────────────────────────────
// PM CHECKLISTS — DETAILED
// ─────────────────────────────────────────
const PM_SECTIONS = [
  {
    icon:'🔧', title:'PLUMBING — FIXTURES & TAPWARE',
    items:[
      {label:'Kitchen sink tap — check for drips, washer condition',sub:'Turn on/off, check under sink for slow drips'},
      {label:'Bathroom basin taps — all checked',sub:'Hot & cold, aerator condition, flow rate'},
      {label:'Shower mixer / rose — flow & temperature',sub:'Check diverter, pressure, rose blockage'},
      {label:'Bath taps & overflow — tested',sub:'Inspect spout, overflow, pop-up waste'},
      {label:'Laundry taps — hot & cold tested',sub:'Check hose connections for swelling or wear'},
      {label:'Outdoor taps — inspected & functional',sub:'Backflow protection fitted?'},
    ]
  },
  {
    icon:'🚽', title:'TOILETS & CISTERNS',
    items:[
      {label:'Toilet flush tested — full & half flush',sub:'Flush valve condition, flush volume'},
      {label:'Cistern inlet valve — no continuous running',sub:'Listen after flush; valve should shut off cleanly'},
      {label:'Pan connector & base seal — no leaks',sub:'Check for damp/staining at base'},
      {label:'Toilet suite connections & isolator — checked',sub:'Isolator operation, flexi hose condition'},
    ]
  },
  {
    icon:'💧', title:'HOT WATER SYSTEM',
    items:[
      {label:'HWS type & age noted',sub:'Storage / instantaneous / heat pump / solar'},
      {label:'Temperature & Pressure Relief valve tested',sub:'Lift lever, check discharge pipe is clear'},
      {label:'Anode condition — visually assessed (if accessible)',sub:'Recommend replacement if >5 years'},
      {label:'HWS connections — no corrosion or drips',sub:'Inlet, outlet, sacrificial anode port'},
      {label:'Hot water temp at outlet tested',sub:'Should reach 50–60°C at tap'},
      {label:'HWS isolation valve operational',sub:'Full turn-off confirmed'},
    ]
  },
  {
    icon:'🌊', title:'DRAINAGE & WASTE',
    items:[
      {label:'Kitchen sink waste — flow tested',sub:'No slow drain, no smell'},
      {label:'Basin wastes — all checked',sub:'Pop-up, plug-hole, P-trap access'},
      {label:'Shower floor waste — flow tested & clean',sub:'Remove and clean if blocked'},
      {label:'Bath waste — tested',sub:'Pop-up or plug, flow rate, odour'},
      {label:'Laundry tub waste — flow & condition',sub:'Check lint trap if fitted'},
      {label:'External drainage — stormwater flow',sub:'Check pits, grates, downpipes for blockage'},
    ]
  },
  {
    icon:'🏗️', title:'PIPES & WATER SUPPLY',
    items:[
      {label:'Main water meter & isolator — located & tested',sub:'Full shut-off confirmed'},
      {label:'Water pressure tested — acceptable range',sub:'Ideal: 200–500 kPa; note if PRV fitted'},
      {label:'Visible pipework — no corrosion or staining',sub:'Under sinks, in roof space if accessible'},
      {label:'PRV / pressure limiting valve — condition',sub:'Tested or note age; replace >10 years'},
      {label:'Water meter reading noted',sub:'Record for client baseline'},
    ]
  },
  {
    icon:'🔥', title:'GAS — APPLIANCES & SUPPLY',
    items:[
      {label:'Gas meter located & condition checked',sub:'No corrosion, clear access, emergency valve operable'},
      {label:'Gas main shutoff — operational',sub:'Full open/close confirmed'},
      {label:'Gas cooktop burners — all lit & tested',sub:'Flame colour, ignition, simmer'},
      {label:'Gas oven — ignition & flame tested',sub:'Check grill if gas'},
      {label:'Gas space heater(s) — tested',sub:'Flame, thermocouple, fan if applicable'},
      {label:'Gas hot water unit — ignition & temperature',sub:'Flue clear, combustion air adequate'},
      {label:'Gas BBQ connection — inspected if present',sub:'Flexible hose, regulator condition'},
    ]
  },
  {
    icon:'🛡️', title:'GAS SAFETY — PIPEWORK & LEAK TEST',
    items:[
      {label:'All visible gas pipework inspected',sub:'No corrosion, adequate support, correct materials'},
      {label:'Flexible gas connectors — condition checked',sub:'No kinks, abrasion or age cracking'},
      {label:'Soap test performed on accessible joints',sub:'All joints, meter connections, appliance inlets'},
      {label:'Electronic leak detector used',sub:'Tested at meter, appliances and connections'},
      {label:'Flue/exhaust checked — draft tested',sub:'Combustion products not spilling indoors'},
      {label:'Gas isolation valve at each appliance',sub:'Each appliance has accessible isolator'},
    ]
  },
  {
    icon:'🏠', title:'ROOF & STORMWATER',
    items:[
      {label:'Roof penetrations — flashings intact',sub:'Soil vent pipes, exhausts, solar'},
      {label:'Gutters — clear & flowing correctly',sub:'No blockage, no overflow marks'},
      {label:'Downpipes — connected and discharging correctly',sub:'To stormwater or soak pit'},
      {label:'Roof water tanks — if present, condition noted',sub:'Inlet screen, overflow, first flush device'},
    ]
  },
  {
    icon:'📝', title:'COMPLETION & SIGN-OFF',
    items:[
      {label:'Client / owner notified of any defects',sub:'Verbal communication & written in notes'},
      {label:'Priority items photographed',sub:'Before/after if work performed'},
      {label:'PM record updated in FlowTrack',sub:'Date, tech, all findings saved'},
      {label:'Next PM date recommended & communicated',sub:'Typical interval: 6–12 months'},
      {label:'Quote provided for any remedial work',sub:'Itemised, delivered same day if possible'},
    ]
  }
];

function buildPMForm() {
  const grid=document.getElementById('pmf-checklist');
  grid.innerHTML=PM_SECTIONS.map((sec,si)=>`
    <div class="pm-section-wrap">
      <div class="pm-section-title">${sec.icon} ${sec.title}</div>
      ${sec.items.map((item,ii)=>`
        <div class="pm-item" id="pmi-${si}-${ii}" onclick="togglePMItem(this,'pmi-${si}-${ii}')">
          <div class="pm-check">✓</div>
          <div class="pm-item-body">
            <div class="pm-item-label">${item.label}</div>
            <div class="pm-item-sub">${item.sub}</div>
            <div onclick="event.stopPropagation()" style="display:flex;gap:10px;align-items:center;margin-top:6px;flex-wrap:wrap">
              <div class="pm-rating">
                <label>Condition:</label>
                <select class="pm-cond-sel" onchange="updatePMProgress()">
                  <option value="">— Not checked</option>
                  <option value="pass">✅ Satisfactory</option>
                  <option value="monitor">⚠️ Monitor</option>
                  <option value="fail">❌ Attention Required</option>
                  <option value="na">N/A</option>
                </select>
              </div>
              <input class="pm-note-input" type="text" placeholder="Note (optional)…" style="flex:1;min-width:160px">
            </div>
          </div>
        </div>
      `).join('')}
    </div>
  `).join('');
  updatePMProgress();
}

function togglePMItem(el) {
  el.classList.toggle('checked');
  updatePMProgress();
}

function updatePMProgress() {
  const items=document.querySelectorAll('#pmf-checklist .pm-item');
  const checked=document.querySelectorAll('#pmf-checklist .pm-item.checked').length;
  const pct=items.length?Math.round((checked/items.length)*100):0;
  document.getElementById('pm-prog-bar').style.width=pct+'%';
  document.getElementById('pm-prog-label').textContent=`${checked} / ${items.length} checked (${pct}%)`;
}

function openNewPM() {
  popClientSelect('pmf-client',null);
  document.getElementById('pmf-date').value=today();
  document.getElementById('pmf-tech').value='';
  document.getElementById('pmf-notes').value='';
  // Set next PM roughly 6 months out
  const next=new Date(); next.setMonth(next.getMonth()+6);
  document.getElementById('pmf-next').value=next.toISOString().split('T')[0];
  buildPMForm();
  open('m-pm');
}

function openNewPMForClient() {
  close('m-detail');
  setTimeout(()=>{
    openNewPM();
    if(currentId) popClientSelect('pmf-client',currentId);
  },200);
}

function savePM() {
  const clientId=document.getElementById('pmf-client').value;
  const date=document.getElementById('pmf-date').value;
  if(!clientId||!date){alert('Select a client and date.');return;}
  const checks=[];
  document.querySelectorAll('#pmf-checklist .pm-item').forEach(el=>{
    const cond=el.querySelector('.pm-cond-sel')?.value||'';
    const note=el.querySelector('.pm-note-input')?.value||'';
    checks.push({
      label:el.querySelector('.pm-item-label').textContent,
      checked:el.classList.contains('checked'),
      condition:cond, note
    });
  });
  const d=DB.get();
  d.pmRecords.push({
    id:uid(), clientId, date,
    tech:document.getElementById('pmf-tech').value.trim(),
    nextDue:document.getElementById('pmf-next').value,
    notes:document.getElementById('pmf-notes').value.trim(),
    checks, createdAt:today(),
    author: stamp()
  });
  DB.set('pmRecords',d.pmRecords);
  logActivity('completed a PM check', (() => { const c=DB.get().clients.find(x=>x.id===clientId); return c?c.fname+' '+c.lname+' — '+c.address:''; })());
  close('m-pm');
  renderPMList();
  updateCounts();
  renderDashboard();
}

function renderPMList() {
  const d=DB.get();
  const container=document.getElementById('pm-records-list');
  const recs=[...d.pmRecords].sort((a,b)=>new Date(b.date)-new Date(a.date));
  if(!recs.length){container.innerHTML='<div class="empty-state" style="margin-top:32px"><div class="empty-icon">✅</div>No PM checklists yet.</div>';return;}
  container.innerHTML=recs.map(pm=>{
    const c=d.clients.find(x=>x.id===pm.clientId);
    const done=pm.checks.filter(x=>x.checked).length;
    const total=pm.checks.length;
    const pct=total?Math.round((done/total)*100):0;
    const flagged=pm.checks.filter(x=>x.condition==='fail'||x.condition==='monitor');
    return `<div class="card" style="margin-bottom:14px">
      <div style="padding:14px 16px;display:flex;align-items:center;gap:14px;border-bottom:1px solid var(--border2)">
        <div style="flex:1">
          <div style="font-weight:700;font-size:14px">${c?c.fname+' '+c.lname:'—'}</div>
          <div style="font-size:12px;color:var(--text3)">${c?c.address:''}</div>
        </div>
        <div style="text-align:right">
          <div class="mono" style="font-size:11px;color:var(--text3)">${fDate(pm.date)}${pm.tech?' · 👷 '+pm.tech:''}</div>
          <div style="margin-top:4px">
            <span class="badge ${pct>=80?'b-green':pct>=50?'b-amber':'b-red'}">${done}/${total} items (${pct}%)</span>
            ${pm.nextDue?`<span class="badge b-blue" style="margin-left:5px">Next: ${fDate(pm.nextDue)}</span>`:''}
          </div>
        </div>
      </div>
      ${pm.notes?`<div style="padding:10px 16px;font-size:13px;color:var(--text2);border-bottom:1px solid var(--border2)">📝 ${pm.notes}</div>`:''}
      ${flagged.length?`<div style="padding:10px 16px;border-bottom:1px solid var(--border2)"><div style="font-size:11px;font-weight:700;letter-spacing:1px;text-transform:uppercase;color:var(--text3);margin-bottom:6px;font-family:JetBrains Mono,monospace">Items Flagged</div><div style="display:flex;flex-wrap:wrap;gap:5px">${flagged.map(f=>`<span class="badge ${f.condition==='fail'?'b-red':'b-amber'}" style="font-size:10px">${f.condition==='fail'?'❌':'⚠️'} ${f.label.slice(0,40)}${f.note?' — '+f.note:''}</span>`).join('')}</div></div>`:''}
      <div style="padding:10px 16px;display:flex;flex-wrap:wrap;gap:4px">
        ${pm.checks.filter(x=>x.checked).map(x=>`<span class="badge b-green" style="font-size:10px">✅ ${x.label.slice(0,35)}</span>`).join('')}
        ${pm.checks.filter(x=>!x.checked).map(x=>`<span class="badge b-grey" style="font-size:10px;opacity:.5">— ${x.label.slice(0,35)}</span>`).join('')}
      </div>
    </div>`;
  }).join('');
}

// ─────────────────────────────────────────
// DASHBOARD
// ─────────────────────────────────────────
function renderDashboard() {
  const d=DB.get();
  const recent=document.getElementById('dash-recent');
  const jobs=[...d.jobs].sort((a,b)=>new Date(b.date)-new Date(a.date)).slice(0,5);
  if(!jobs.length){recent.innerHTML='<div class="empty-state">No activity yet. Add a client to begin.</div>';
  } else {
    recent.innerHTML=jobs.map(j=>{
      const c=d.clients.find(x=>x.id===j.clientId);
      return `<div class="activity-item" onclick="openClientDetail('${j.clientId}')">
        <div class="ai-icon">🔧</div>
        <div class="ai-body"><div class="ai-title">${c?c.fname+' '+c.lname:'—'}</div><div class="ai-sub">${j.type} · ${j.desc?j.desc.slice(0,45):''}</div></div>
        <div class="ai-time">${fDate(j.date)}</div>
      </div>`;
    }).join('');
  }
  const pmAlerts=document.getElementById('dash-pm-alerts');
  const over=d.clients.filter(c=>{
    const last=d.pmRecords.filter(p=>p.clientId===c.id).sort((a,b)=>new Date(b.date)-new Date(a.date))[0];
    const days=last?(new Date()-new Date(last.date))/(864e5):9999;
    return days>120;
  }).slice(0,5);
  if(!over.length){pmAlerts.innerHTML='<div class="empty-state">All PM checks current. ✅</div>';
  } else {
    pmAlerts.innerHTML=over.map(c=>{
      const last=d.pmRecords.filter(p=>p.clientId===c.id).sort((a,b)=>new Date(b.date)-new Date(a.date))[0];
      const days=last?Math.round((new Date()-new Date(last.date))/(864e5)):null;
      return `<div class="activity-item" onclick="openClientDetail('${c.id}')">
        <div class="ai-icon">⚠️</div>
        <div class="ai-body"><div class="ai-title">${c.fname} ${c.lname}</div><div class="ai-sub">${c.address}</div></div>
        <span class="badge b-amber">${days?days+'d ago':'Never'}</span>
      </div>`;
    }).join('');
  }
}

// ─────────────────────────────────────────
// CSV EXPORT
// ─────────────────────────────────────────
function exportCSV() {
  const clients=DB.get().clients;
  if(!clients.length){alert('No clients.');return;}
  const h=['Name','Address','Phone','Email','Type','Property','Year Built','Referred By','Notes','Created'];
  const rows=clients.map(c=>[c.fname+' '+c.lname,c.address,c.phone,c.email,c.type,c.proptype,c.yearbuilt,c.referred,c.notes,c.createdAt].map(v=>'"'+(v||'').replace(/"/g,'""')+'"').join(','));
  const csv=[h.join(','),...rows].join('\n');
  const b=new Blob([csv],{type:'text/csv'});
  const u=URL.createObjectURL(b);
  const a=document.createElement('a');a.href=u;a.download='clients.csv';a.click();URL.revokeObjectURL(u);
}

// ─────────────────────────────────────────
// CALENDAR
// ─────────────────────────────────────────
let calYear = new Date().getFullYear();
let calMonth = new Date().getMonth(); // 0-based
let calDayPanel = null;

const CAL_MONTHS = ['January','February','March','April','May','June','July','August','September','October','November','December'];

function calNav(dir) {
  calMonth += dir;
  if(calMonth > 11){ calMonth = 0; calYear++; }
  if(calMonth < 0){ calMonth = 11; calYear--; }
  renderCalendar();
}

function calGoToday() {
  const now = new Date();
  calYear = now.getFullYear();
  calMonth = now.getMonth();
  renderCalendar();
}

function renderCalendar() {
  const d = DB.get();
  const grid = document.getElementById('cal-grid');
  const label = document.getElementById('cal-month-label');
  if(!grid) return;

  label.textContent = CAL_MONTHS[calMonth] + ' ' + calYear;

  // Update legend with tech colours
  const legend = document.getElementById('cal-legend');
  if(legend) {
    const techs = getTechs();
    const techChips = techs.map(t => {
      const col = techColour(t);
      return `<span style="display:flex;align-items:center;gap:5px;font-size:11px;color:var(--text2)"><span style="width:10px;height:10px;border-radius:3px;background:${col};display:inline-block;flex-shrink:0"></span>${t.name}</span>`;
    }).join('');
    legend.innerHTML = `${techChips}<span style="color:var(--text3);font-size:10px;margin-left:4px">· ● done ○ outstanding ◑ in progress</span>`;
  }

  // Build a map: dateString → [jobs]
  const jobMap = {};
  d.jobs.forEach(j => {
    if(!j.date) return;
    if(!jobMap[j.date]) jobMap[j.date] = [];
    jobMap[j.date].push(j);
  });

  // First day of month
  const firstDay = new Date(calYear, calMonth, 1).getDay(); // 0=Sun
  const daysInMonth = new Date(calYear, calMonth+1, 0).getDate();
  const daysInPrevMonth = new Date(calYear, calMonth, 0).getDate();

  const todayStr = today();
  const cells = [];

  // Pad start with prev month days
  for(let i = firstDay - 1; i >= 0; i--) {
    const day = daysInPrevMonth - i;
    const month = calMonth === 0 ? 11 : calMonth - 1;
    const year = calMonth === 0 ? calYear - 1 : calYear;
    const ds = `${year}-${String(month+1).padStart(2,'0')}-${String(day).padStart(2,'0')}`;
    cells.push({ day, ds, thisMonth: false });
  }

  // This month
  for(let day = 1; day <= daysInMonth; day++) {
    const ds = `${calYear}-${String(calMonth+1).padStart(2,'0')}-${String(day).padStart(2,'0')}`;
    cells.push({ day, ds, thisMonth: true });
  }

  // Pad end with next month days
  const remaining = 42 - cells.length;
  for(let day = 1; day <= remaining; day++) {
    const month = calMonth === 11 ? 0 : calMonth + 1;
    const year = calMonth === 11 ? calYear + 1 : calYear;
    const ds = `${year}-${String(month+1).padStart(2,'0')}-${String(day).padStart(2,'0')}`;
    cells.push({ day, ds, thisMonth: false });
  }

  grid.innerHTML = cells.map(cell => {
    const jobs = jobMap[cell.ds] || [];
    const isToday = cell.ds === todayStr;
    const maxShow = 3;
    const visible = jobs.slice(0, maxShow);
    const extra = jobs.length - maxShow;

    const bars = visible.map(j => {
      const c = d.clients.find(x => x.id === j.clientId);
      const name = c ? c.fname + ' ' + c.lname : j.type;
      // Use tech colour if available, else fall back to status colour
      const barColour = j.author && j.author.techColour
        ? j.author.techColour
        : j.status === 'completed' ? 'var(--green)' : j.status === 'in-progress' ? 'var(--amber)' : 'var(--red)';
      const statusDot = j.status === 'completed' ? '●' : j.status === 'in-progress' ? '◑' : '○';
      return `<div class="cal-job-bar" style="background:${barColour}" onclick="event.stopPropagation();calOpenDay('${cell.ds}')" title="${name} — ${j.type} (${j.author?j.author.techName:'?'})">${statusDot} ${name}</div>`;
    }).join('');

    const moreBtn = extra > 0
      ? `<div class="cal-more" onclick="event.stopPropagation();calOpenDay('${cell.ds}')">+${extra} more</div>`
      : '';

    return `<div class="cal-cell${isToday?' today':''}${!cell.thisMonth?' other-month':''}${jobs.length?' has-jobs':''}" onclick="calOpenDay('${cell.ds}')">
      <div class="cal-day-num">${cell.day}</div>
      ${bars}
      ${moreBtn}
    </div>`;
  }).join('');
}

function calOpenDay(dateStr) {
  // Remove existing panel
  calCloseDayPanel();

  const d = DB.get();
  const jobs = d.jobs.filter(j => j.date === dateStr).sort((a,b) => {
    // sort by arrival time then job id
    if(a.arriveTime && b.arriveTime) return a.arriveTime.localeCompare(b.arriveTime);
    return 0;
  });

  const [y, m, day] = dateStr.split('-');
  const label = `${parseInt(day)} ${CAL_MONTHS[parseInt(m)-1]} ${y}`;

  const panel = document.createElement('div');
  panel.className = 'cal-day-panel';
  panel.id = 'cal-day-panel';

  const completed = jobs.filter(j => j.status === 'completed').length;
  const notDone = jobs.filter(j => j.status !== 'completed').length;

  panel.innerHTML = `
    <div class="cal-day-panel-head">
      <div>
        <div style="font-family:'Barlow Condensed',sans-serif;font-size:20px;font-weight:800;letter-spacing:.5px">${label}</div>
        <div style="font-size:12px;color:var(--text3);margin-top:2px">
          ${jobs.length} job${jobs.length!==1?'s':''} scheduled
          ${completed?` &nbsp;·&nbsp; <span style="color:var(--green)">${completed} completed</span>`:''}
          ${notDone?` &nbsp;·&nbsp; <span style="color:var(--red)">${notDone} outstanding</span>`:''}
        </div>
      </div>
      <div style="display:flex;gap:8px;align-items:center">
        <button class="btn btn-primary btn-xs" onclick="calCloseDayPanel();openAddJobForDate('${dateStr}')">＋ Book</button>
        <button class="modal-close" onclick="calCloseDayPanel()">✕</button>
      </div>
    </div>
    <div class="cal-day-panel-body">
      ${jobs.length === 0
        ? `<div class="empty-state" style="padding:20px"><div class="empty-icon">📅</div>No jobs on this day.<br><a href="#" onclick="calCloseDayPanel();openAddJobForDate('${dateStr}')" style="color:var(--copper)">Book a job</a></div>`
        : jobs.map(j => {
            const c = d.clients.find(x => x.id === j.clientId);
            const cls = j.status === 'completed' ? 'b-green' : j.status === 'in-progress' ? 'b-amber' : 'b-red';
            const barCls = j.status === 'completed' ? 'completed' : j.status === 'in-progress' ? 'in-progress' : 'not-done';
            return `<div style="background:var(--surface2);border:1px solid var(--border2);border-radius:8px;padding:12px 14px;margin-bottom:8px;cursor:pointer;transition:border-color .15s" onclick="calCloseDayPanel();openClientDetail('${j.clientId}')" onmouseover="this.style.borderColor='var(--copper)'" onmouseout="this.style.borderColor='var(--border2)'">
              <div style="display:flex;align-items:flex-start;justify-content:space-between;gap:10px">
                <div style="flex:1">
                  <div style="display:flex;align-items:center;gap:8px;margin-bottom:4px">
                    <div style="width:10px;height:10px;border-radius:2px;flex-shrink:0;background:${j.status==='completed'?'var(--green)':j.status==='in-progress'?'var(--amber)':'var(--red)'}"></div>
                    <span style="font-weight:700;font-size:13.5px">${c ? c.fname+' '+c.lname : '—'}</span>
                    <span class="badge b-grey" style="font-size:10px">${j.jobId||j.id.slice(0,8).toUpperCase()}</span>
                  </div>
                  <div style="font-size:12px;color:var(--text3);margin-bottom:4px">${c ? c.address : ''}</div>
                  <div style="display:flex;align-items:center;gap:8px;flex-wrap:wrap">
                    <span class="tag">${j.type}</span>
                    ${statusBadge(j.status)}
                    ${j.arriveTime||j.finishTime?`<span style="font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--text3)">⏱ ${j.arriveTime||'?'} – ${j.finishTime||'?'}</span>`:''}
                  </div>
                  ${j.tech?`<div style="font-size:11px;color:var(--text3);margin-top:5px">👷 ${j.tech}${j.licence?' · Lic: '+j.licence:''}</div>`:''}
                  ${j.desc?`<div style="font-size:12px;color:var(--text2);margin-top:5px;line-height:1.4">${j.desc.slice(0,100)}${j.desc.length>100?'…':''}</div>`:''}
                  ${j.author?`<div style="margin-top:7px;display:flex;align-items:center;gap:6px"><div style="width:3px;height:16px;background:${j.author.techColour||'#888'};border-radius:2px"></div><span style="font-size:10px;font-weight:700;color:${j.author.techColour||'#888'}">Filed by ${j.author.techName}</span></div>`:''}
                </div>
              </div>
              ${j.urgent?`<div style="margin-top:8px;padding:7px 10px;background:var(--red-dim);border-radius:5px;font-size:11px;color:var(--red)">🚨 ${j.urgent.slice(0,80)}${j.urgent.length>80?'…':''}</div>`:''}
            </div>`;
          }).join('')
      }
    </div>`;

  document.body.appendChild(panel);
  calDayPanel = panel;

  // Click outside to close
  setTimeout(()=>{
    document.addEventListener('click', calOutsideClick);
  }, 50);
}

function calOutsideClick(e) {
  const panel = document.getElementById('cal-day-panel');
  if(panel && !panel.contains(e.target)) {
    calCloseDayPanel();
  }
}

function calCloseDayPanel() {
  const panel = document.getElementById('cal-day-panel');
  if(panel) panel.remove();
  calDayPanel = null;
  document.removeEventListener('click', calOutsideClick);
}

function openAddJobForDate(dateStr) {
  openAddJob();
  setTimeout(()=>{ document.getElementById('jf-date').value = dateStr; }, 50);
}

// ─────────────────────────────────────────
// TECH SIGN-IN SYSTEM
// ─────────────────────────────────────────
let currentTech = null;
let pinBuffer = '';
let pinTarget = null;

// Unique colour palette for technicians
const TECH_COLOURS = [
  '#e8821a', // copper  (default admin)
  '#3fb950', // green
  '#58a6ff', // blue
  '#f85149', // red
  '#d29922', // amber
  '#a371f7', // purple
  '#39d353', // lime
  '#ff7b72', // salmon
  '#79c0ff', // sky
  '#ffa657', // orange
  '#7ee787', // mint
  '#f78166', // coral
];

function techColour(tech) {
  if(tech && tech.colour) return tech.colour;
  // Fallback: derive from index in techs array
  const techs = getTechs();
  const idx = techs.findIndex(t => t.id === (tech&&tech.id));
  return TECH_COLOURS[Math.max(0,idx) % TECH_COLOURS.length];
}

const DEFAULT_TECHS = [
  { id:'admin', name:'Admin', role:'Owner', pin:'1234', admin:true, email:'', colour:'#e8821a' }
];

function getTechs() {
  try {
    const d = DB.load();
    return d.techs && d.techs.length ? d.techs : DEFAULT_TECHS;
  } catch { return DEFAULT_TECHS; }
}

function saveTechs(techs) {
  const d = DB.load();
  d.techs = techs;
  DB.save(d);
}

function techInitials(name) {
  return name.split(' ').map(w=>w[0]).join('').toUpperCase().slice(0,2);
}

// Stamp authorship: returns {techId, techName, techColour, at}
function stamp() {
  if(!currentTech) return null;
  return {
    techId: currentTech.id,
    techName: currentTech.name,
    techColour: techColour(currentTech),
    at: new Date().toISOString()
  };
}

// Render a small inline "written by" chip
function authorChip(s, prefix) {
  if(!s || !s.techName) return '';
  const pre = prefix || 'Logged by';
  const time = s.at ? ' · ' + new Date(s.at).toLocaleDateString('en-AU',{day:'2-digit',month:'short',year:'numeric'}) : '';
  return `<span style="display:inline-flex;align-items:center;gap:5px;font-size:10px;font-weight:700;color:white;background:${s.techColour||'#888'};padding:2px 8px;border-radius:10px;letter-spacing:.3px">${pre}: ${s.techName}${time}</span>`;
}

// Render a colour bar strip for calendar / list views
function authorBar(s, label) {
  if(!s || !s.techName) return '';
  return `<div style="display:flex;align-items:center;gap:6px;margin-top:5px">
    <div style="width:4px;height:100%;min-height:30px;background:${s.techColour||'#888'};border-radius:2px;flex-shrink:0;align-self:stretch"></div>
    <span style="font-size:10px;color:${s.techColour||'#888'};font-weight:700">${s.techName}${label?' — '+label:''}</span>
  </div>`;
}

function renderLoginTechGrid() {
  const techs = getTechs();
  const grid = document.getElementById('login-tech-grid');
  if(!techs.length) {
    grid.innerHTML = '<div style="grid-column:1/-1;text-align:center;color:var(--text3);font-size:13px;padding:16px">No technicians set up.<br>Tap Manage Technicians below.</div>';
    return;
  }
  grid.innerHTML = techs.map(t => {
    const col = techColour(t);
    return `<div class="tech-btn" onclick="selectTech('${t.id}')" style="border-color:${col}22">
      <div class="tech-btn-avatar" style="background:${col}22;color:${col};border-color:${col}44">${techInitials(t.name)}</div>
      <div class="tech-btn-name">${t.name}</div>
      <div class="tech-btn-role">${t.role||'Technician'}</div>
      <div style="width:100%;height:3px;background:${col};border-radius:2px;margin-top:6px"></div>
    </div>`;
  }).join('');
}

function selectTech(id) {
  const techs = getTechs();
  const tech = techs.find(t => t.id === id);
  if(!tech) return;
  pinTarget = tech;
  pinBuffer = '';
  updatePinDots();
  const col = techColour(tech);
  const av = document.getElementById('pin-screen-avatar');
  av.textContent = techInitials(tech.name);
  av.style.background = col+'22';
  av.style.color = col;
  av.style.borderColor = col+'66';
  document.getElementById('pin-screen-name').textContent = tech.name;
  document.getElementById('pin-screen-role').textContent = tech.role || 'Technician';
  document.getElementById('pin-error').textContent = '';
  // Show forgot PIN link if they have an email
  const forgotEl = document.getElementById('forgot-pin-link');
  if(forgotEl) {
    forgotEl.style.display = tech.email ? 'block' : 'none';
  }
  document.getElementById('login-step-select').style.display = 'none';
  document.getElementById('login-step-pin').style.display = 'block';
}

function backToSelect() {
  pinBuffer = '';
  pinTarget = null;
  document.getElementById('pin-error').textContent = '';
  document.getElementById('login-step-pin').style.display = 'none';
  document.getElementById('login-step-select').style.display = 'block';
}

function pinKey(digit) {
  if(pinBuffer.length >= 4) return;
  pinBuffer += digit;
  updatePinDots();
  if(pinBuffer.length === 4) {
    setTimeout(checkPin, 120);
  }
}

function pinBackspace() {
  if(!pinBuffer.length) return;
  pinBuffer = pinBuffer.slice(0,-1);
  updatePinDots();
  document.getElementById('pin-error').textContent = '';
  for(let i=0;i<4;i++) document.getElementById('pd'+i).classList.remove('error');
}

function updatePinDots() {
  for(let i=0;i<4;i++){
    const dot = document.getElementById('pd'+i);
    dot.classList.toggle('filled', i < pinBuffer.length);
    dot.classList.remove('error');
    if(pinTarget) dot.style.background = i < pinBuffer.length ? techColour(pinTarget) : '';
    if(pinTarget && i < pinBuffer.length) dot.style.borderColor = techColour(pinTarget);
    else if(dot && !dot.classList.contains('filled')) dot.style.borderColor = '';
  }
}

function checkPin() {
  if(!pinTarget) return;
  if(pinBuffer === pinTarget.pin) {
    currentTech = pinTarget;
    updateSidebarTech();
    document.getElementById('login-screen').classList.add('hidden');
    document.body.style.overflow = '';
    pinBuffer = '';
    pinTarget = null;
  } else {
    for(let i=0;i<4;i++) document.getElementById('pd'+i).classList.add('error');
    document.getElementById('pin-error').textContent = 'Incorrect PIN — try again';
    setTimeout(()=>{ pinBuffer = ''; updatePinDots(); }, 700);
  }
}

// ── FORGOT PIN ──
function forgotPin() {
  if(!pinTarget || !pinTarget.email) return;
  const tech = pinTarget;
  const col = techColour(tech);
  // Build a mailto link that pre-fills with PIN reset info
  // Since this is a local app with no server, we open an email with the PIN visible
  // (owner-only feature — this is intentional for a local tool)
  const allTechs = getTechs();
  const adminTech = allTechs.find(t => t.admin && t.email);
  const subject = encodeURIComponent(`Plumb.Inc FlowTrack — PIN Reset for ${tech.name}`);
  const body = encodeURIComponent(
    `Hi ${tech.name},\n\nA PIN reset was requested for your Plumb.Inc FlowTrack Pro account.\n\nYour current PIN is: ${tech.pin}\n\nTo change your PIN, ask your Admin to update it in Manage Technicians.\n\n— Plumb.Inc FlowTrack Pro`
  );
  const mailTo = `mailto:${tech.email}?subject=${subject}&body=${body}`;
  // Show confirm dialog first
  const confirmed = confirm(`Send PIN reminder to ${tech.email}?\n\nThis will open your email app.`);
  if(confirmed) window.open(mailTo, '_blank');
}

function updateSidebarTech() {
  if(!currentTech) return;
  const footer = document.querySelector('.co-chip');
  if(!footer) return;
  const col = techColour(currentTech);
  footer.innerHTML = `
    <div class="co-avatar" style="background:${col};font-size:14px">${techInitials(currentTech.name)}</div>
    <div>
      <div class="co-name">${currentTech.name}</div>
      <div class="co-role" style="color:${col}">${currentTech.role||'Technician'}${currentTech.admin?' · Admin':''}</div>
    </div>
    <button onclick="signOut()" style="margin-left:auto;background:none;border:none;color:var(--text3);font-size:18px;cursor:pointer;padding:4px 6px;touch-action:manipulation;min-height:36px" title="Sign out">⇥</button>
  `;
  // Also update topbar accent to tech colour
  document.querySelector('.topbar').style.borderBottomColor = col+'66';
}

function signOut() {
  if(!confirm('Sign out of FlowTrack?')) return;
  currentTech = null;
  pinBuffer = '';
  pinTarget = null;
  document.getElementById('login-step-pin').style.display = 'none';
  document.getElementById('login-step-select').style.display = 'block';
  renderLoginTechGrid();
  document.getElementById('login-screen').classList.remove('hidden');
  document.querySelector('.topbar').style.borderBottomColor = '';
  // Reset sidebar footer
  const footer = document.querySelector('.co-chip');
  if(footer) footer.innerHTML = `
    <div class="co-avatar">P</div>
    <div><div class="co-name">Plumb.Inc</div><div class="co-role">FlowTrack Pro</div></div>
  `;
}

// ── MANAGE TECHS ──
function openManageTechs() {
  // Only block non-admins who are already signed in
  if(currentTech && !currentTech.admin) {
    alert('Admin access required to manage technicians.');
    return;
  }
  renderTechsList();
  ['nt-name','nt-role','nt-pin','nt-email'].forEach(id=>document.getElementById(id).value='');
  document.getElementById('nt-admin').value='0';
  document.getElementById('nt-colour').value=TECH_COLOURS[getTechs().length % TECH_COLOURS.length];
  openModal('m-techs');
}

function renderTechsList() {
  const techs = getTechs();
  const container = document.getElementById('techs-list');
  if(!techs.length){container.innerHTML='<div style="color:var(--text3);font-size:13px;padding:8px 0">No technicians added yet.</div>';return;}
  container.innerHTML = techs.map(t=>{
    const col = techColour(t);
    return `<div class="tech-row">
      <div class="tech-row-avatar" style="background:${col}22;color:${col};border:2px solid ${col}44">${techInitials(t.name)}</div>
      <div class="tech-row-info">
        <div class="tech-row-name">${t.name} ${t.admin?'<span class="badge b-copper" style="font-size:9px">Admin</span>':''}</div>
        <div class="tech-row-pin">${t.role||'Technician'}${t.email?' · '+t.email:''}</div>
      </div>
      <div style="width:10px;height:32px;background:${col};border-radius:3px;flex-shrink:0"></div>
      <button class="btn btn-danger btn-xs" onclick="deleteTech('${t.id}')">🗑</button>
    </div>`;
  }).join('');
}

function addTech() {
  const name = document.getElementById('nt-name').value.trim();
  const pin = document.getElementById('nt-pin').value.trim();
  if(!name || !pin){ alert('Name and PIN are required.'); return; }
  if(!/^\d{4}$/.test(pin)){ alert('PIN must be exactly 4 digits.'); return; }
  const techs = getTechs();
  if(techs.find(t=>t.pin===pin)){ alert('That PIN is already in use. Choose a different one.'); return; }
  const colour = document.getElementById('nt-colour').value || TECH_COLOURS[techs.length % TECH_COLOURS.length];
  techs.push({
    id: uid(),
    name,
    role: document.getElementById('nt-role').value.trim() || 'Technician',
    pin,
    email: document.getElementById('nt-email').value.trim(),
    admin: document.getElementById('nt-admin').value === '1',
    colour
  });
  saveTechs(techs);
  renderTechsList();
  ['nt-name','nt-role','nt-pin','nt-email'].forEach(id=>document.getElementById(id).value='');
}

function deleteTech(id) {
  if(!confirm('Remove this technician?')) return;
  const techs = getTechs().filter(t=>t.id!==id);
  saveTechs(techs);
  renderTechsList();
  renderLoginTechGrid();
}

// Pre-fill tech name in job & PM forms when logged in
function prefillTechName() {
  if(!currentTech) return;
  const jt = document.getElementById('jf-tech');
  const jl = document.getElementById('jf-licence');
  if(jt && !jt.value) jt.value = currentTech.name;
  if(jl && !jl.value && currentTech.licence) jl.value = currentTech.licence;
}

// ─────────────────────────────────────────
// ACTIVITY LOG — who did what
// ─────────────────────────────────────────
function logActivity(action, detail) {
  if(!currentTech) return;
  const d = DB.load();
  const log = d.activityLog || [];
  log.unshift({
    id: uid(),
    techId: currentTech.id,
    techName: currentTech.name,
    techColour: techColour(currentTech),
    action,
    detail,
    at: new Date().toISOString()
  });
  // Keep last 200 entries
  d.activityLog = log.slice(0, 200);
  DB.save(d);
}

// Activity log render (called when opening manage techs activity tab)
function renderActivityLog() {
  const d = DB.load();
  const log = d.activityLog || [];
  const container = document.getElementById('activity-log-list');
  if(!log.length){
    container.innerHTML='<div class="empty-state"><div class="empty-icon">📋</div>No activity recorded yet.</div>';
    return;
  }
  container.innerHTML = log.map(entry=>{
    const col = entry.techColour || '#888';
    const dt = entry.at ? new Date(entry.at).toLocaleString('en-AU',{day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit'}) : '—';
    return `<div style="display:flex;align-items:flex-start;gap:12px;padding:9px 0;border-bottom:1px solid var(--border2)">
      <div style="width:4px;flex-shrink:0;min-height:36px;background:${col};border-radius:2px;margin-top:2px"></div>
      <div style="width:30px;height:30px;border-radius:7px;background:${col}22;color:${col};font-family:'Barlow Condensed',sans-serif;font-size:14px;font-weight:800;display:flex;align-items:center;justify-content:center;flex-shrink:0">${techInitials(entry.techName)}</div>
      <div style="flex:1">
        <div style="font-size:13px;font-weight:700;color:var(--text)">${entry.techName} <span style="font-weight:400;color:var(--text2)">${entry.action}</span></div>
        ${entry.detail?`<div style="font-size:12px;color:var(--text3);margin-top:2px">${entry.detail}</div>`:''}
      </div>
      <div style="font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--text3);white-space:nowrap;flex-shrink:0">${dt}</div>
    </div>`;
  }).join('');
}


// ─────────────────────────────────────────
// SWIPE DOWN TO CLOSE (mobile)
// ─────────────────────────────────────────
(function(){
  let touchStartY = 0;
  let touchTarget = null;
  document.addEventListener('touchstart', e => {
    const modal = e.target.closest('.modal');
    if(modal) { touchStartY = e.touches[0].clientY; touchTarget = modal.closest('.overlay'); }
  }, {passive:true});
  document.addEventListener('touchend', e => {
    if(!touchTarget) return;
    const dy = e.changedTouches[0].clientY - touchStartY;
    if(dy > 80) { closeModal(touchTarget.id); }
    touchTarget = null;
  }, {passive:true});
})();

// ─────────────────────────────────────────
// INIT
// ─────────────────────────────────────────
window.addEventListener('DOMContentLoaded',()=>{
  document.getElementById('topbar-btn').style.display='none';
  renderDashboard();
  updateCounts();
  renderLoginTechGrid();
  document.body.style.overflow = 'hidden';

  // Colour picker live preview
  const cp = document.getElementById('nt-colour');
  const prev = document.getElementById('colour-preview');
  if(cp && prev) {
    cp.addEventListener('input', ()=>{ prev.style.background = cp.value; });
  }
});
</script>
<!-- ═══ LOGIN SCREEN ═══ -->
<div id="login-screen">
  <svg viewBox="0 0 280 75" xmlns="http://www.w3.org/2000/svg" style="width:260px;max-width:88%;display:block;margin:0 auto 4px">
      <defs>
        <linearGradient id="lgFlame" x1="0" y1="0" x2="0" y2="1">
          <stop offset="0%" stop-color="#f59410"/>
          <stop offset="55%" stop-color="#e8520a"/>
          <stop offset="100%" stop-color="#bf2e08"/>
        </linearGradient>
        <linearGradient id="lgInner" x1="0" y1="0" x2="0" y2="1">
          <stop offset="0%" stop-color="#ffd878"/>
          <stop offset="100%" stop-color="#f59410"/>
        </linearGradient>
        <linearGradient id="lgBlue" x1="0" y1="0" x2="0" y2="1">
          <stop offset="0%" stop-color="#2255a0"/>
          <stop offset="100%" stop-color="#0f3575"/>
        </linearGradient>
      </defs>
      <!-- Blue background ellipse -->
      <ellipse cx="34" cy="40" rx="25" ry="30" fill="url(#lgBlue)" opacity="0.85"/>
      <!-- Flame outer -->
      <path d="M34 7 C34 7 11 28 11 45 C11 58.5 21.5 68 34 68 C46.5 68 57 58.5 57 45 C57 28 34 7 34 7Z" fill="url(#lgFlame)"/>
      <!-- Flame inner -->
      <path d="M34 25 C34 25 21 40 21 48 C21 55 27 61 34 61 C41 61 47 55 47 48 C47 40 34 25 34 25Z" fill="url(#lgInner)"/>
      <!-- White core -->
      <ellipse cx="34" cy="50" rx="7" ry="9" fill="white" opacity="0.88"/>
      <!-- PLUMB -->
      <text x="68" y="46" font-family="'Arial Black',Arial" font-weight="900" font-size="32" fill="white" letter-spacing="1">PLUMB</text>
      <!-- inc italic -->
      <text x="214" y="46" font-family="'Arial Black',Arial" font-weight="900" font-size="32" fill="#f59410" letter-spacing="0.5" font-style="italic">inc</text>
      <!-- tagline -->
      <text x="68" y="64" font-family="Arial,sans-serif" font-size="11.5" fill="#5a9ad4" letter-spacing="2.5">PLUMBING &amp; GAS</text>
    </svg>
  <div class="login-sub">FlowTrack Pro · 0423 601 010</div>

  <!-- Step 1: choose tech -->
  <div class="login-card" id="login-step-select">
    <div class="login-card-title">Who's signing in?</div>
    <div class="login-card-sub">Select your name to continue</div>
    <div class="tech-select-grid" id="login-tech-grid"></div>
    <div style="text-align:center">
      <button class="login-back-btn" onclick="openManageTechs()" style="color:var(--text3);font-size:11px;letter-spacing:1px;font-family:'JetBrains Mono',monospace">⚙ MANAGE TECHNICIANS</button>
    </div>
  </div>

  <!-- Step 2: enter PIN -->
  <div class="login-card" id="login-step-pin" style="display:none">
    <div style="display:flex;align-items:center;gap:12px;margin-bottom:16px">
      <div class="tech-btn-avatar" id="pin-screen-avatar"></div>
      <div>
        <div class="login-card-title" id="pin-screen-name"></div>
        <div class="login-card-sub" style="margin-bottom:0" id="pin-screen-role"></div>
      </div>
    </div>
    <div class="pin-label">Enter your PIN</div>
    <div class="pin-dots" id="pin-dots">
      <div class="pin-dot" id="pd0"></div>
      <div class="pin-dot" id="pd1"></div>
      <div class="pin-dot" id="pd2"></div>
      <div class="pin-dot" id="pd3"></div>
    </div>
    <div class="login-error" id="pin-error"></div>
    <div class="pin-pad">
      <div class="pin-key" onclick="pinKey('1')">1</div>
      <div class="pin-key" onclick="pinKey('2')">2</div>
      <div class="pin-key" onclick="pinKey('3')">3</div>
      <div class="pin-key" onclick="pinKey('4')">4</div>
      <div class="pin-key" onclick="pinKey('5')">5</div>
      <div class="pin-key" onclick="pinKey('6')">6</div>
      <div class="pin-key" onclick="pinKey('7')">7</div>
      <div class="pin-key" onclick="pinKey('8')">8</div>
      <div class="pin-key" onclick="pinKey('9')">9</div>
      <div class="pin-key" style="visibility:hidden"></div>
      <div class="pin-key" onclick="pinKey('0')">0</div>
      <div class="pin-key del" onclick="pinBackspace()">⌫</div>
    </div>
    <button id="forgot-pin-link" onclick="forgotPin()" style="display:none;background:none;border:none;color:var(--text3);font-size:12px;cursor:pointer;margin-top:10px;padding:8px;touch-action:manipulation;width:100%;text-align:center;text-decoration:underline">Forgot PIN? Send to my email</button>
    <button class="login-back-btn" onclick="backToSelect()">← Back</button>
  </div>
</div>

<!-- ═══ MODAL: MANAGE TECHNICIANS ═══ -->
<div class="overlay" id="m-techs">
  <div class="modal modal-lg">
    <div class="modal-head">
      <div class="modal-title">Manage Technicians</div>
      <button class="modal-close" onclick="closeModal('m-techs')">✕</button>
    </div>
    <div class="tabs">
      <div class="tab active" onclick="tab(this,'mt-tab-team')">Team</div>
      <div class="tab" onclick="tab(this,'mt-tab-activity')">Activity Log</div>
    </div>
    <div class="pane active" id="mt-tab-team" style="max-height:70vh;overflow-y:auto;-webkit-overflow-scrolling:touch">
      <div class="alert alert-amber" style="font-size:12px">⚠️ Admin only — set names, PINs, colours and emails. The coloured bar is each tech's unique identifier across the whole app.</div>
      <div id="techs-list" style="margin-bottom:16px"></div>
      <div style="background:var(--surface2);border:1px solid var(--border2);border-radius:var(--radius);padding:16px">
        <div style="font-size:12px;font-weight:700;color:var(--text);margin-bottom:12px">Add New Technician</div>
        <div class="grid2" style="gap:10px">
          <div class="fg"><label>Full Name *</label><input id="nt-name" placeholder="e.g. Jake Smith"></div>
          <div class="fg"><label>Role</label><input id="nt-role" placeholder="e.g. Plumber, Apprentice"></div>
          <div class="fg"><label>PIN (4 digits) *</label><input id="nt-pin" type="password" inputmode="numeric" maxlength="4" placeholder="••••"></div>
          <div class="fg"><label>Email (for PIN reset)</label><input id="nt-email" type="email" placeholder="jake@plumbinc.com.au"></div>
          <div class="fg">
            <label>Colour Tag</label>
            <div style="display:flex;gap:8px;align-items:center">
              <input id="nt-colour" type="color" value="#3fb950" style="width:44px;height:36px;padding:2px;border-radius:6px;border:1px solid var(--border);background:var(--surface2);cursor:pointer">
              <div id="colour-preview" style="flex:1;height:36px;border-radius:6px;background:#3fb950;display:flex;align-items:center;justify-content:center;font-family:'Barlow Condensed',sans-serif;font-size:14px;font-weight:700;color:white;letter-spacing:1px">PREVIEW</div>
            </div>
          </div>
          <div class="fg"><label>Admin Access?</label>
            <select id="nt-admin"><option value="0">No — Technician</option><option value="1">Yes — Admin / Owner</option></select>
          </div>
        </div>
        <button class="btn btn-primary" style="margin-top:12px;width:100%" onclick="addTech()">＋ Add Technician</button>
      </div>
    </div>
    <div class="pane" id="mt-tab-activity" style="max-height:70vh;overflow-y:auto;-webkit-overflow-scrolling:touch">
      <div id="activity-log-list"></div>
    </div>
    <div class="modal-foot">
      <button class="btn btn-secondary" onclick="closeModal('m-techs')">Done</button>
    </div>
    <div class="modal-sticky-close"><button onclick="closeModal('m-techs')">✕ Close</button></div>
  </div>
</div>

</body>
</html>
